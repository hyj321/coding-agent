"""Capability + Decision + Cost + Context + Improvement eval package.

Import submodules directly (e.g. ``from evals.suite import run_offline_suite``)
to avoid pulling optional live-eval dependencies at package import time.
"""
