# 编程智能体（Code Agent）计划书

> 本文档随开发进度持续更新。每次改代码后，请在文末「进度日志」（第 12 节）追加一条记录，并勾选对应任务。长短期记忆见 **第 11 节**；**Tool 编排**见 **第 13 节**；**Skill（任务方法包）**见 **第 14 节**；**步数 / Token 效率**见 **第 15 节**；**减少无效推理 / 无效行动**见 **第 16 节**；**安全 / 权限 / Verification**见 **第 17 节**。  
> **交付后质量总纲（八维标准 + 优先改造序）：** 见根目录 [`Agent质量标准与改进路线.md`](./Agent质量标准与改进路线.md)——后续调研与改造优先按该文档推进，细节实现仍回链本章第 11～17 节。

**截止时间：** 2026-09-02 24:00（北京时间）  
**当前阶段：** 开发主体已完成，进入 **Day4 交付**（录视频 + README.txt + 推送/打包提交）  
**目标形态：** 自研 Agent Harness + DeepSeek tool calling + 本地工具箱 + Todo 特色 + CLI/Web 双入口 + 可观测 timeline  

**学术诚信提醒：** 开源项目仅作架构学习，禁止整仓复制或「套壳」提交；核心循环、工具执行、上下文与安全逻辑必须自己实现并能答辩。

---

## 0. 已完成总览（截至 2026-08-29）

> 本节汇总「现在已经做成什么」，方便对照题目与答辩。未完成项见第 6 节 Day4。

### 0.1 对照题目硬性要求

| 题目要求 | 状态 | 落点 |
|----------|------|------|
| 自主读写文件、执行命令 | ✅ | `read_file` / `write_file` / `edit_file` / `run_shell` 等，本地执行 |
| 对话历史与上下文管理 | ✅ | `messages` 拼接；`trim_messages`；工具输出截断；transcript |
| 工具定义与本地执行 | ✅ | `ToolRegistry`（schema + handler）；无 Code Interpreter |
| 模型输出解析 | ✅ | 解析 `tool_calls` / 参数 JSON / `tool_call_id` |
| 循环终止条件 | ✅ | 无 tool_calls 结束 / `max_steps` / Ctrl+C |
| 错误处理 | ✅ | 工具错误回传模型；沙箱；hard-deny；approval |
| 不用 agent 框架 | ✅ | 仅用 OpenAI 兼容客户端调 DeepSeek |
| 凭据不入库 | ✅ | `.env` + `.gitignore`；`.env.example` 无密钥 |
| 公开 Git 仓库 | ✅ | https://github.com/hyj321/coding-agent （后续改动需再 push） |

### 0.2 已实现能力清单

**核心 Harness**

- Agent 主循环（`src/agent/loop.py`）+ 可选 `on_event` 结构化事件  
- DeepSeek / OpenAI 兼容 LLM 客户端（`src/llm/client.py`）  
- 配置：`DEEPSEEK_API_KEY` / `BASE_URL` / `MODEL` / `MAX_STEPS` / `MAX_MESSAGES` / `APPROVAL`  

**工具（7 个）**

- `read_file` / `write_file` / `edit_file` / `list_dir` / `glob` / `run_shell` / `todo_write`  

**安全与稳定性**

- 工作目录路径沙箱  
- `--approval auto|ask|never` + 危险命令 hard-deny  
- **三级风险元数据**（`risk_level` / `is_readonly`）+ 敏感路径（`.env` / 密钥）始终 Deny  
- 上下文裁剪、工具输出截断、transcript JSON 落盘  
- **ACON 简化 Context Manager**：观测压缩 + 分层折叠（`CONTEXT_TOKEN_BUDGET`）  

**特色**

- **Plan-then-Act**：`todo_write` + system prompt「先计划再执行」  

**交互入口**

- **CLI**：`python -m src.main -w demos "..."`  
- **Web UI**：`python -m src.web` → http://127.0.0.1:7860  
  - 仿现代聊天产品布局（非套壳第三方 agent，仍调同一套 loop）  
  - SSE 推送步骤；步骤卡片（完成可折叠、进行中居中高亮）  
  - 左侧栏固定 + Recent 独立滚动；**session 级历史**（一大轮对话一条记录，可多轮续写）  
  - Open folder → 更新 workdir；右侧 Files 树 + 新窗口看文件；底部 Changed files + 新旧 diff  
  - Todo / Plan 面板；Markdown 富文本；Reset demos；单任务锁（并发第二次 409）  

**演示与质量**

- 定稿演示：`demos/greeter.py` + `greeter_test.py`（见 `demos/DEMO.md`）  
- 另有 `buggy_calc.py` 等；`POST /api/demos/reset` 可恢复有意 bug  
- 离线冒烟：`python -m scripts.smoke_v1`  
- 仓库 README 含架构说明与运行方式  

### 0.3 尚未完成（交付相关）

- [ ] 本地录制 ≤2 分钟演示视频  
- [x] 撰写提交用 `README.txt`（≤1000 汉字）  
- [x] 将最新代码 `git push` 到 GitHub（保持提交历史）  
- [ ] 打「姓名.zip」（仅 README.txt + 视频）并上传表格  

### 0.4 怎么跑（速查）

```powershell
conda activate codeagent   # 或 .venv
cd G:\codeagent
pip install -r requirements.txt
# 配置 .env 中的 DEEPSEEK_API_KEY

# CLI
python -m src.main -w demos --approval auto "阅读 greeter_test.py，修复 greeter.py…先用 todo_write…"

# Web
python -m src.web
# 浏览器打开 http://127.0.0.1:7860 ，建议 Ctrl+F5
```

---

## 1. 题目要求摘要

个人独立实现一个编程智能体：通过与大语言模型交互，自主读写文件、执行命令，完成编程任务。

### 硬性约束

- 禁止在现成 agent 产品上封装界面
- 禁止使用 agent 框架 / SDK（LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等）
- 允许：模型厂商 API 客户端、OpenAI 兼容网关、原生 tool calling
- 禁止依赖服务端托管的代码执行或文件工具（Code Interpreter、Files API 等）
- 必须自研：对话历史与上下文管理、工具定义与本地执行、模型输出解析、循环终止条件、错误处理
- API Key 等凭据只能走环境变量或未入库配置，不得出现在仓库 / README / 视频中

### 提交物

1. **公开 Git 仓库**（题目发布后新建，保留完整提交历史）
2. **README.txt**（≤1000 汉字）：仓库地址、如何运行、特色功能等
3. **视频**（≤2 分钟，mp4，≤200 MB）：演示真实编程任务 + 简要讲解实现
4. 最终打成「姓名.zip」（仅含 README.txt + 视频）提交

### 面试关注点

能否解释 agent 为何这样运转，并为设计决策辩护。

---

## 2. 开源调研：别人怎么做、我们学什么

> 调研日期：2026-08-29。结论：优秀作品的共同点不是「功能最多」，而是 **loop 极简 + harness 分层清晰 + 安全/上下文/可观测三者至少做实**。

### 2.1 参考项目一览

| 项目 | 链接 | 体量/定位 | 优点 | 对我们的启示 | 4 天内不要照搬 |
|------|------|-----------|------|--------------|----------------|
| **ai-coding-agent**（你提到的） | [RivaanRanawat/ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) | 功能较全的 Claude Code 风格 | 模块划分清楚（agent/client/tools/context/safety/hooks/ui）；有审批策略、危险命令检测、会话存档、循环检测 | 目录可参考；「审批 + 安全 + transcript」值得学 | MCP、子 agent、复杂 Hooks、完整 TUI 命令体系 |
| **CoreCoder** | [he-yufeng/CoreCoder](https://github.com/he-yufeng/CoreCoder) | ~1000 行「nanoGPT of coding agents」 | 把本质说透：while 循环 + 若干工具；工具可扩展；可读性极强 | **主学习对象**：先读懂再自写；八工具里优先学 bash/read/write/edit/glob/grep/todo | 子 agent 工具、完整 CLI 包装 |
| **mini-coding-agent** | [rasbt/mini-coding-agent](https://github.com/rasbt/mini-coding-agent) | 教学向 harness | 明确六块：workspace 快照、稳定 prompt、结构化工具+权限、transcript/memory、有界委托 | 面试可用「六组件」叙事；`--approval` / `--max-steps` 设计很干净 | 子 agent 委托、复杂 memory |
| **learn-claude-code / mini-claude-code** | [shareAI-lab](https://github.com/shareAI-lab/mini-claude-code) / [Learn Claude Code](https://learn-cc-agent.vercel.app/en/) | 渐进教程 v0→v4 | v0 只有 bash → v1 四工具 → v2 Todo → v3 子 agent → v4 Skills | **开发顺序就按这个爬坡**，不要第一天做满 | Skills / 子 agent（时间不够就停在 v1+Todo） |
| **agent-zero-to-hero** | [KeWang0622/agent-101](https://github.com/KeWang0622/agent-101) | 课程式从零搭建 | 强调「整个 loop 就几行」；无框架；多 provider | 证明作业完全可自研；章节式理解利于答辩 | 不必做完 20 章 |
| **Harness 六层文章** | [FutureAGI: 6 Core Layers](https://futureagi.com/blog/how-to-build-a-coding-agent-harness/) | 架构说明 | Provider / Loop / Tool Registry / Context / Sandbox / Permissions | 用「六层 harness」写进 README 与面试稿 | Linux bubblewrap 级沙箱（Windows 上改用路径沙箱即可） |
| **Edd Mann 实践文** | [Around the Loop](https://eddmann.com/posts/around-the-loop-building-a-coding-agent-harness-in-python/) | 个人完整 harness 复盘 | 权限用 hook 切入；Skills；会话持久化；子 agent 取最简端 | 「权限检查插在工具执行前」比堆功能更专业 | 完整插件协议、深嵌套子 agent |

### 2.2 业界共识（几乎所有项目都承认）

核心只有这一段（OpenAI / Anthropic 协议细节不同，语义相同）：

```text
while True:
    response = model(messages, tools)
    if 没有 tool_calls:
        return 最终文本
    本地执行 tools → 把结果 append 进 messages
    # 必须有 max_steps，防止死循环
```

**难点不在 loop，而在 harness：** 工具边界、上下文膨胀、安全审批、失败恢复、过程可观测。

### 2.3 可直接借鉴的「好设计」（按优先级）

| 优先级 | 设计点 | 学自何处 | 我们怎么落地 |
|--------|--------|----------|--------------|
| P0 | 工具注册表：schema + handler 成对 | CoreCoder / FutureAGI | `tools/base.py` 注册，loop 只 `dispatch(name, args)` |
| P0 | 多终止条件：正常结束 / max_steps / 用户中断 | 几乎所有人 | Day1 就写进 loop |
| P0 | 工具错误变成字符串回传模型 | 所有能自我修复的 agent | `try/except` → `Error: ...` |
| P0 | 过程日志：每步打印工具名与摘要 | ai-coding-agent / 视频需求 | 彩色或前缀日志，方便录屏 |
| P1 | `edit` 优于整文件 `write` | CoreCoder / Claude Code 系 | Day2 加 `edit_file` |
| P1 | 工具输出截断 + 历史裁剪 | rasbt / FutureAGI compaction | 先截断 stdout；再丢弃最旧中间轮 |
| P1 | 工作区沙箱 + 危险命令确认 | ai-coding-agent / FutureAGI Permissions | Windows 上做路径限制 + 命令黑名单/ask |
| P1 | Transcript 落盘（JSON/JSONL） | MindStudio harness / rasbt | 每次 run 一份，便于复盘与视频 |
| P2 | Todo / 显式计划（防跑偏） | learn-claude-code v2、CoreCoder `todo_write` | **推荐作为特色 B** |
| P2 | Workspace 快照注入 system prompt | rasbt | 启动时列顶层目录 + 说明 cwd |
| P2 | 循环检测（重复同一工具调用） | ai-coding-agent | 小而美，面试好讲 |
| P3 | grep/glob | CoreCoder | Day2–3 有空再加 |
| 不做 | MCP / 多子 agent / **Skills 生态（市场级）** / 复杂 TUI | 各大型项目 | 时间成本高；**轻量 3～5 个本地 Skill 见第 14 节**（≠ 生态） |

### 2.4 别人「不全」或「过大」的地方 → 我们如何做得更好

针对 [ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) 这类「看起来全但未必适合作业」的项目：

| 常见问题 | 更好的做法（本项目） |
|----------|----------------------|
| 功能面铺太开（MCP、子 agent、hooks…），核心反而难讲清 | **深度 > 广度**：六层 harness 每层都能画图讲解 |
| 缺「真实可复现演示任务」 | 自带 `demos/` 小项目 + 固定任务脚本，视频一次过 |
| 安全停留在开关，缺少默认安全路径 | **默认沙箱**（只能碰工作目录），危险操作为 opt-in |
| 上下文策略含糊，长任务易崩 | 明确两级：① 工具输出截断 ② 历史 K 轮保留；写进 README |
| 换模型不友好 | 从第一天就用 **OpenAI 兼容客户端 + DeepSeek**，只换 env |
| 文档像功能清单，不像设计说明 | README/面试稿强调 **为什么这样停、为什么错误要回传、为什么要裁剪** |

**差异化口号（写进最终 README 可用）：**

> 一个能讲清楚的小型 coding agent harness：DeepSeek tool calling + 本地工具执行 + 可观测循环 + 默认路径沙箱；特色做深「Todo 规划」或「测试驱动修复」，而不是堆 MCP。

### 2.5 建议精读顺序（编码前 2–3 小时）

1. [Learn Claude Code 核心 loop](https://learn-cc-agent.vercel.app/en/)（建立直觉）  
2. [FutureAGI 六层 harness](https://futureagi.com/blog/how-to-build-a-coding-agent-harness/)（建立分层词汇）  
3. 浏览 [CoreCoder](https://github.com/he-yufeng/CoreCoder) 的 `agent` 主循环与 `tools/`（看「最小能跑」长什么样）  
4. 扫一眼 [rasbt/mini-coding-agent](https://github.com/rasbt/mini-coding-agent) 的六组件命名（方便写文档）  
5. [ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) 只看 `safety` / `context` / 工具列表，**不要抄全仓结构**

---

## 3. 系统组件（对齐「六层 Harness」）

业界常用六层；我们在 Windows + 4 天约束下做**精简落地版**：

```
用户输入（CLI）
   ↓
┌─────────────────────────────────────────┐
│  Delivery：CLI / 日志 / 可选确认         │
├─────────────────────────────────────────┤
│  Permissions：路径沙箱 + 危险命令策略     │
├─────────────────────────────────────────┤
│  Context：system 组装 + 历史裁剪 + 截断   │
├─────────────────────────────────────────┤
│  Tools：Registry（schema ↔ handler）     │
├─────────────────────────────────────────┤
│  Agent Loop：call → tool_calls → append  │
├─────────────────────────────────────────┤
│  Provider：DeepSeek（OpenAI 兼容）       │
└─────────────────────────────────────────┘
```

| 组件 | 职责 | 面试一句话 |
|------|------|------------|
| Provider | chat + tools API | 只当客户端，不含 agent 逻辑 |
| Agent Loop | 调模型 → 执行工具 → 写回历史 | 没有框架，就是 while + 终止条件 |
| Tool Registry | schema 与本地函数成对 | 模型只出意图，副作用在本地 |
| Context | 防爆上下文、组装 system | 截断输出 + 保留最近 K 轮 |
| Permissions | 限制能碰什么 | 默认工作目录沙箱，危险命令可确认 |
| CLI | 可观测与交互 | 每一步工具调用都看得见 |

---

## 4. 功能清单

### 4.1 必备功能（MVP = learn-claude-code 的 v1 水平）

- [x] Agent 主循环（调模型 → 执行 tool_calls → 写回历史 → 再调）
- [x] 终止条件：无工具调用的最终回复 / 达到最大步数 / 用户中断
- [x] 工具：`read_file`
- [x] 工具：`write_file`
- [x] 工具：`list_dir`（或启动时 workspace 快照二选一，建议都有）
- [x] 工具：`run_shell`
- [x] 对话历史按 system / user / assistant / tool 正确拼接（含 `tool_call_id`）
- [x] 工具失败时把错误返回给模型（不直接崩溃）
- [x] 配置：`DEEPSEEK_API_KEY`（或通用 `API_KEY`）/ `BASE_URL` / `MODEL` 走环境变量
- [x] 可指定工作目录
- [x] 终端打印工具名、参数、结果摘要（过程可见）

### 4.2 强烈建议（稳定性与作品感 = harness 合格线）

- [x] `edit_file` / search_replace（避免整文件重写）
- [x] 路径沙箱（限制在 project root 内）—(V1 已做)
- [x] 工具输出截断（尤其 shell）
- [x] 上下文裁剪（保留 system + 最近 K 轮）
- [x] 危险操作二次确认（`--approval ask|auto|never`）
- [x] 运行 transcript 落盘（JSON）
- [x] 启动时把 cwd 顶层结构写入 system prompt（轻量 workspace 快照）

### 4.3 特色功能（只选 1 个做深）

调研后重新排序的候选：

- [x] **A. Todo / Plan-then-Act（最推荐）** — 已实现 `todo_write` + system prompt 规划约束（P0：粗粒度 + 同轮批工具）  
- [ ] **B. 测试驱动修复** — 改完自动跑测试，失败继续修；视频效果极强  
- [x] **C. 循环检测** — 相同 tool+args：≥3 警告、≥5 `loop_detected`（`LoopGuard`）  
- [x] D. 多 tool_calls 一次执行 + 清晰分步日志 — loop 已支持；P0 prompt 强制「能批就批」

**已选定特色：** A. Todo / Plan-then-Act（`todo_write`）

### 4.4 扩展功能（有时间再做；P3）

- [x] `grep` / `glob` 搜索工具 —（Day2 已加 `glob`；`grep` 仍可选）
- [x] 简单摘要式 compaction（超长任务）— ACON 简化版 Context Manager：观测压缩 + 分层折叠 + history summary
- [ ] 流式输出
- [x] 会话 save/resume — Web session_*.json 多轮续写
- [x] Web UI（非必须）— `python -m src.web`：SSE 步骤卡片、Todo/Files 右栏、session 历史、Open folder、Changed files diff、Reset demos、Markdown、步骤折叠 / 进行中居中
- [x] **长短期记忆增强（交付后 / 有余力）** — 详见 **第 11 节**（**P0–P2 已做**；云端 embedding / 厂商 KV compaction 仍依赖网关能力）

---

## 5. 技术选型

| 项 | 选择 | 备注 |
|----|------|------|
| 语言 | **Python 3.11+**（已定） | 与多数教学仓一致，工具好写 |
| API | **DeepSeek**（OpenAI 兼容 tool calling，已定） | `base_url=https://api.deepseek.com`；默认 `deepseek-v4-flash` |
| SDK | 官方 `openai` 客户端即可 | 允许使用；不算 agent 框架 |
| 交互 | **CLI + Web UI** | Web 可选；核心仍是同一套 harness |
| Agent 框架 | **不使用** | 符合题目要求 |

### 5.1 当前目录结构

```
codeagent/
  README.md / 计划书.md / requirements.txt / .env.example
  src/
    main.py                 # CLI
    config.py
    agent/                  # loop / context / permissions / transcript
    llm/client.py
    tools/                  # base / filesystem / shell / todo
    web/                    # FastAPI + static UI
  demos/                    # greeter、buggy_calc、DEMO.md …
  scripts/smoke_v1.py
  transcripts/              # gitignore
```

### 5.2 扩展性约定

- 新工具 = 实现函数 + 注册 schema，主循环不改  
- 换模型 = 只改 `BASE_URL` / `MODEL`  
- 新模式（Todo / 测后修复）= 新工具或 loop 外薄包装，不推翻重写  
- Permissions 独立模块：以后加规则不必改工具内部逻辑  

---

## 6. 四天周期计划（按调研后的爬坡路径）

对齐 learn-claude-code：**v0/v1 → 稳定 harness → 一个特色 → 交付**。

### Day 0–1（8/29–8/30）：MVP 跑通（= v1）

**目标：** 一句话任务能让 agent 自己写文件并执行成功。

- [x] 新建公开 Git 仓库并完成首次提交（https://github.com/hyj321/coding-agent ）  
- [x] `config` + DeepSeek LLM client（非流式 chat + tools）  
- [x] Tool registry + `read` / `write` / `list_dir` / `run_shell`  
- [x] Agent loop + max_steps + 错误回传 + 日志  
- [x] CLI 入口可用（`python -m src.main`）  
- [x] 路径沙箱  
- [x] 离线冒烟：`python -m scripts.smoke_v1` 通过  
- [x] **端到端验收**：`python -m src.main -w demos "...创建 hello.py 并运行"` → 3 步完成，`stopped_reason=completed`  

**验收：** 无人工干预完成「写一个脚本并运行成功」。 ✅ 2026-08-29 已通过

### Day 2（8/31）：Harness 合格线

- [x] `edit_file`  
- [x] 工具输出截断 + 上下文裁剪（`trim_messages` / `MAX_MESSAGES`）  
- [x] workspace 快照进 system prompt  
- [x] approval / 危险命令策略（`auto|ask|never` + hard-deny）  
- [x] transcript 保存（`transcripts/*.json`）  
- [x] `glob` 搜索工具  

**验收：** 能对已有小项目修 bug / 加小功能，中间报错能自我恢复。  
✅ 2026-08-29：修复 `demos/buggy_calc.py` — `read_file` → `edit_file` → `run_shell` 验证通过（4 步）。
### Day 3（9/1）：一个特色做深 + 演示定稿

- [x] 实现选定特色：**Todo / Plan-then-Act（`todo_write`）**  
- [x] 定稿 `demos/` 演示任务（见 `demos/DEMO.md`：`greeter.py` + `greeter_test.py`）  
- [ ] 试录视频：日志清晰、任务有头有尾（**待你本地录制**）  
- [x] 补一段「架构说明」到仓库 README  

**验收：** 同一演示任务稳定复现；你能不看稿讲清六层里你实现了哪几层。  
✅ 2026-08-29 端到端：先 `todo_write` → 读文件 → `edit_file` → 跑测试 `ok` → todo 全完成（5 步）。演示源码已恢复为有意 bug，便于复录。

### Day 4（9/2）：交付

- [x] README.txt（≤1000 字）  
- [x] 仓库 README（安装、环境变量、示例、设计要点、Web 说明）— 已有，提交前可再润色  
- [ ] 视频（≤2 分钟）  
- [x] `.gitignore` 含 `.env`（勿提交密钥）  
- [x] 将最新代码 push 到 GitHub → 打 zip 提交  
- [x] **当天不再加新大功能**（记忆 P0–P2 已收束）

---

## 7. 演示任务（已定稿）

**设计原则：** 任务必须用工具；包含读代码 → 改代码 → 跑命令验证；并体现 Todo 规划。

**最终演示任务：** 修复 `demos/greeter.py`，使 `greeter_test.py` 全部通过；过程中使用 `todo_write`。

**复现命令：**

```powershell
conda activate codeagent
cd G:\codeagent
python -m src.main -w demos --approval auto --max-steps 20 "阅读 greeter_test.py，修复 greeter.py 使测试全部通过。用 todo_write 写 3～5 条阶段计划（可与读文件同轮），阶段完成再更新 todo；最后运行 python greeter_test.py 验证。"
```

详见 `demos/DEMO.md`（含步数基线）。

---

## 8. 面试预案（设计决策）

开发过程中把「为什么」写清楚，面试直接用：

1. **主循环如何停止？**  
   _答：无 tool_calls 的最终回复 / 达到 max_steps / 用户中断 / **loop_detected**（相同 tool+args 连续 ≥5 次）/ **retry_exhausted**（同 failure_key 失败满级，默认 3）/ **goal_met_forced**（测试已过仍继续改文件）。≥3 次相同调用先警告；测试全绿或 todo 全完成会先注入 final nudge。_

2. **工具结果为什么必须进 messages？**  
   _答：模型看不见本地副作用，只能通过返回内容感知；`tool_call_id` 用于对齐请求与结果。_

3. **上下文爆了怎么办？**  
   _答：两级。① 工具观测压缩（pytest 等只留失败卡片，`compress_tool_result`）；② ACON 思路的 Context Manager：超 `CONTEXT_TOKEN_BUDGET`（默认 **32000**）时折叠中间轮为 Historical Context，保留目标 / Current State（焦点文件、错误、todo）/ 最近工具轮。完整对话另存 `transcripts/`（含 `memory` 快照）。_

4. **为什么不用 LangChain？**  
   _答：题目禁止；自研可完全控制循环、错误回传与终止；也与 CoreCoder 等教学路径一致。_

5. **如何防止危险操作？**  
   _答：路径必须落在 workdir；`PermissionGate` 对 `rm -rf /` 等 hard-deny；三级风险（Low 自动 / Medium 视策略 / High 确认或禁止）+ 敏感路径（`.env` / SSH Key）硬拦；按 todo 阶段 **收窄可见工具**（Least Privilege）；Web 上 High 一律 Deny。完成判定不靠模型自述，而靠 Harness 的 pytest / exit code 证据（CompletionGate，见第 17 节）。_

6. **特色 Todo 为什么有用？**  
   _答：模型容易中途跑偏；`todo_write` 把计划变成可观察状态，同一 loop 内边做边勾选，答辩时能指着日志讲「先计划后执行」。_

7. **你和「功能很多的开源 agent」差在哪？**  
   _答：故意不做 MCP/子 agent，把 Todo、安全与可观测做深，保证每一行都能答辩。_

8. **长短期记忆怎么做的？还缺什么？**  
   _答：短期 = Context Manager（观测压缩 → MicroCompact stub → 超预算折叠 → 重注根状态；稳定前缀/可变后缀）；ACON-lite 用失败对收紧 `.agent/compress_guideline.json`。会话 = 续写瘦身 + working_memory.json。长期 = MEMORY.md + memory_search（关键词）+ rag_search（本地 TF–IDF）。厂商 KV-cache / 服务端 compaction：已挂 cache hint，真实命中看网关；本地 fold 作 fallback。未做：云端 embedding、ACON 离线蒸馏小压缩器。_

9. **Tool 编排怎么做的？模型怎么知道调哪个工具？**  
   _答：核心是原生 tool calling 的 ReAct 循环（Reason→Action→Observation），不是手写 if-else 路由。Registry 把 schema 交给模型；PermissionGate 在执行前拦风险。Todo（粗粒度 Plan-then-Act）+ 同轮批工具指引减少空步与盲目 list/read。**已做循环检测**（3 警告 / 5 停）；后续可加 Tool 元数据、按阶段收窄可见工具集——详见第 13 / 15 节。_

10. **Tool 和 Skill 有什么区别？你们做 Skill 吗？**
    _答：Tool 是原子能力（读/写/跑命令）；Skill 是完成一类任务的可复用方法（如 debugging 的步骤剧本）。Todo 是「这一次任务」的运行时计划；Skill 是「这类任务」的先验流程。**已做轻量版**：`debugging` / `testing` / `refactoring`；system 只挂 name+description（L1）；匹配后 `load_skill` 或**关键词预注入**拉正文（L2，Anthropic/SkillReducer 渐进披露）。未做 80K SkillRouter / references 分层——详见第 14 节。_

11. **为什么一步任务要跑很多步？怎么省 token / 提速？**  
    _答：一步 = 一次 LLM 往返。压缩管住单步 token；**P0 已落地**：system/todo 强制同轮批工具 + 3～5 条粗阶段 todo + 循环检测。P1 再做切片读 / grep / 催 final——详见第 15 节。_

12. **怎么防止 Agent 瞎转圈（重复读同一文件、失败后原样重试）？**  
    _答：这和 Context 压缩不是一回事。**P0+P1 已落地**：`LoopGuard`（去重 / 连续警告停）；同 fingerprint 失败 streak；`RetryPolicy`（failure_key 阶梯 1→2 换策→3 `retry_exhausted`）；`TaskState` + failed strategies 注入；测试全绿 / todo 全完成催 FINAL，再改则 `goal_met_forced`。详见第 16 节。_

---

## 9. 风险与对策

| 风险 | 对策 | 状态 |
|------|------|------|
| 被开源项目带偏，功能贪多 | 锁定 P0/P1；特色只选 1 个；MCP/子 agent 不做 | ✅ 已遵守 |
| 模型不听话乱调工具 | 清晰 system prompt + 工具 schema；Todo 约束步骤 | ✅ 已落地 |
| Shell 输出过长 | 截断 + 提示用更精确命令 | ✅ 已截断 |
| 时间不够 | 死守 Day1 MVP；Day3 只做特色 | ✅ 主体完成 |
| Key 泄漏 | 环境变量 + 提交前检查 | ⚠️ 每次 push 前再查 |
| 视频超时 | 脚本化演示；中间过程加速 | 待录制 |
| 整段抄袭开源 | 只学架构，自写实现；能讲清每一模块 | 待遵守 |

---

## 10. 待确认事项

- [x] 确认语言：Python  
- [x] 确认模型：DeepSeek（flash 开发 / pro 演示）  
- [x] 确认特色功能：A Todo / B 测后修复 / C 循环检测 — **已选 A**  
- [x] 确认演示任务（见第 7 节 / `demos/DEMO.md`）  
- [x] 确认公开仓库平台（GitHub / Gitee）与仓库名 — `https://github.com/hyj321/coding-agent`  
- [x] 配置本地 `.env` 并完成 V1 端到端验收  

---

## 11. 长短期记忆：调研结论与后续改造清单

> 调研日期：2026-08-29。用途：交付后或有余力时按 P0→P1→P2 改；**Day4 交付前勿再开大坑**。  
> 心智模型：**记忆 = context engineering**（决定哪些 token 进窗口），不是无限上下文。

### 11.1 业界共识分层

| 层级 | 类比 | 放什么 | 本项目现状 |
|------|------|--------|------------|
| **Working / Core（短）** | RAM | 当前目标、焦点文件、最近工具、todo、最近错误 | ✅ `ContextManager`（Current State + Recent + 观测压缩） |
| **Episodic / Recall（中）** | 日记 | 完整对话可回放/续写 | ✅ `transcripts/session_*.json` + Web 同会话 `prior_messages` |
| **Semantic / Archival（长）** | 硬盘 | 跨会话稳定事实、偏好、项目约定 | ✅ 项目级 `MEMORY.md`（开局注入 / 结束追加） |
| **Compaction** | GC | 窗口满了摘要/裁剪 | ✅ 超 `CONTEXT_TOKEN_BUDGET` 折叠为 Historical Context |

**重要区分：** RAG 是把外存捞回窗口的一种手段，**不等于记忆本身**；记忆还要写、更新、压缩。Coding agent 里「主动 `read_file` / `glob`」也是检索。

### 11.2 值得学的设计（精读）

| 思路 | 核心做法 | 链接 | 对我们的启示 |
|------|----------|------|--------------|
| **MemGPT / Letta** | Core blocks 常驻；Archival 向量检索；agent 用工具自改记忆 | [Agent Memory](https://www.letta.com/blog/agent-memory/) | 长期用「可编辑块」比先上向量库更贴作业 |
| **Claude Code Compaction** | MicroCompact（清旧 tool 大输出）→ Session Memory 摘要 → Full LLM compact；规则放 `CLAUDE.md` 压缩后仍重注 | [Context window](https://code.claude.com/docs/en/context-window) / [Inside Compaction](https://y-agent.github.io/inside-claude-code/04-context-compaction.html) | **多级压缩 + 压缩后重注根状态**；稳定前缀利 Prompt Cache |
| **Raschka / Coding Agent 组件** | Full transcript（永不丢）+ Working memory（小而精喂模型）双轨 | [Components](https://magazine.sebastianraschka.com/p/components-of-a-coding-agent) | 我们 transcript 已有；缺独立 working memory 文件 |
| **OpenAI Cookbook** | Compaction（长会话）与 Memory（跨 run 可复用教训）分开 | [Memory & Compaction](https://developers.openai.com/cookbook/examples/agents_sdk/building_reliable_agents_memory_compaction) | 会话内压缩 ≠ 跨任务长期记忆 |
| **ACON** | 观测压缩 + 历史压缩；失败对优化压缩 guideline（完整版） | [论文](https://arxiv.org/abs/2510.00615) / [代码](https://github.com/microsoft/acon) | **已简化落地** `compress.py` + `context_manager.py`；不做离线优化循环 |
| **HiAgent / HyMem / HMAT** | 按子目标切块；计划与执行轨迹隔离；Working/Episodic/Semantic | HiAgent / HyMem 等论文 | Todo 完成一项时可触发「阶段压缩」 |
| **BYOHarness** | Compaction / Memory / Prompt caching 分章练习 | [byoharness.dev](http://www.byoharness.dev/) | 改造时可对照章节改 harness |

### 11.3 「TUI / 缓存优化」实际指什么

多数不是画终端的技巧，而是 **API Prompt Caching / 前缀稳定**：

1. **前缀稳定**：system、tools schema、项目规则放最前且少改字节  
2. **后缀可变**：Current State、最近 tool、用户新话放最后  
3. **先砍观测再 LLM 总结**：先 stub 大 stdout，再 compact（省钱）  
4. **压缩后重注**：规则 / 焦点文件从磁盘再注入，不依赖摘要「记得住」

现状：system 在单次 run 内缓存（工作区快照冻结）；Current State 作为**可变后缀**挂在消息末尾，不再插在 task 后面把前缀冲掉（P1 已改）。

### 11.4 RAG 该用在哪

| 适合 | 不适合硬上向量 RAG |
|------|-------------------|
| 大仓库语义搜、旧 session 语义召回、项目文档 | 当前报错、刚改文件、todo（应在 Working） |
| | 每步必遵的规则（应进 system / MEMORY.md） |

优先顺序：**文件工具检索 → 关键词搜 transcript → 再考虑 embedding**。

### 11.5 现状操作速查（改代码前先读）

```text
短期（单次 run）
  工具结果 → compress_tool_result → messages
  每步 → ContextManager.prepare_messages
    （稳定 system 前缀 + 末尾可变 working-memory 后缀；超预算折叠）
  todo → completed → 阶段压缩进 history_summary
  折叠后 → 重注 MEMORY + focus_files + 未完成 todo
  结束 → working_memory.json + transcript.memory；规则模板追加 MEMORY.md

会话（同 Web 聊天多轮）
  前端 session_id → load_session → build_continue_context
    （memory 快照 + 最近 K + 原任务）→ 再跑 loop；磁盘 messages 仍追加全量
  New task / Explore → 新 session；可读 MEMORY.md / working_memory.json

长期（跨会话）
  MEMORY.md 开局注入；memory_search 关键词搜 MEMORY + transcripts
  向量语义检索：本地 TF–IDF `rag_search`（P2）；云端 embedding 仍可选
```

关键文件：

- `src/agent/compress.py` — 观测压缩 + MicroCompact stub/summary/full  
- `src/agent/acon_guideline.py` — 失败对 → compress guideline  
- `src/agent/context_manager.py` — 分层 + microcompact + 折叠 + 重注  
- `src/agent/memory.py` — MEMORY.md + working_memory.json + keyword search  
- `src/agent/rag.py` — 本地 TF–IDF 索引 / rag_search  
- `src/agent/cache_policy.py` — prompt-cache 钩子 + 本地 compaction fallback  
- `src/tools/memory_search.py` / `src/tools/rag_search.py`  
- `src/agent/loop.py` / `src/web/runner.py` / `src/llm/client.py`  
- 配置：`CONTEXT_TOKEN_BUDGET` / `--context-budget`
- 模板：`MEMORY.md.example`（运行时 `.agent/*` 记忆文件已 gitignore）

### 11.6 后续改造清单（下次按这个做）

#### P0 — 性价比最高（建议下次优先）

- [x] **项目级 `MEMORY.md`（或 `.agent/MEMORY.md`）**  
  - 任务结束：把「改了哪些文件、结论、坑」追加写入（可用规则模板，不必每次调模型）  
  - 新任务开始：读前 N 行 / 最近一段，注入 System 或 Current State  
  - 落点建议：`context_manager.py` + `runner`/`loop` 收尾钩子；gitignore 可选保留模板  
- [x] **阶段压缩（对齐 Todo）**  
  - 某 todo → `completed` 时，将该项相关旧 tool 轨迹收成 3～5 条 bullet 进 `history_summary`  
- [x] **续写瘦身**  
  - Web 下一轮不要默认整包 `prior_messages`；改为：`memory` 快照 + 最近 K 条消息 + 原任务  

#### P1 — 体验与成本

- [x] **Prompt 布局：稳定前缀 + 可变后缀**（利 DeepSeek/兼容网关的 cache，若有）  
- [x] **独立 `working_memory.json`**：与 full transcript 双轨（Raschka 模式）  
- [x] **`memory_search` 工具**：先关键词/grep 搜 `transcripts/` 与 `MEMORY.md`，再考虑向量  
- [x] **压缩后重注根状态**：折叠后强制再注入 MEMORY + focus_files + 未完成 todo  

#### P2 — 重，可答辩提「未做 / 下一步」

- [x] **向量 RAG（代码库或历史 session）** — 本地 TF–IDF `rag_search`（无云端 embedding / 无 LangChain）  
- [x] **完整 ACON：失败对 → 更新压缩 guideline** — `.agent/compress_guideline.json`（不做离线蒸馏小模型）  
- [x] **MicroCompact 分级** — stub → summary → full；超 75% budget 先 stub 旧 tool 再 fold  
- [x] **KV-cache / 服务端 compaction** — `cache_policy` 挂 prompt_cache_key；无厂商端点时本地 fold fallback
### 11.7 改造时注意（防踩坑）

1. **交付期**：第 6 节 Day4「当天不再加新大功能」——记忆增强默认放交付后。  
2. **勿用框架**：可用自研小工具 + 文件；不要引入 LangChain Memory。  
3. **答辩一句话**：短记忆靠压缩与分层；长记忆靠可编辑外存 + 检索注入；RAG 是手段不是全部。  
4. **改完后**：更新本节勾选、`README` 一行、冒烟加 MEMORY 读写用例、进度日志追加一条。

---

## 12. 进度日志

> 每次修改代码后追加一条。格式：日期 — 做了什么 — 勾选了哪些任务 — 下一步。

### 2026-08-30（三十五）安全权限 P1 落地（可见集 + Evidence + Web High Deny）

- **`tool_visibility`**：按 in-progress todo / goal 推断 explore|edit|verify|full；每步收窄 `openai_tools(names=…)`；`TOOL_VISIBILITY=off` 可关。  
- **`completion_gate`**：改过文件且 `COMPLETION_MODE=evidence` 时，无测试绿则拒 `completed` 并注入催测；`EVIDENCE_NUDGE_MAX` 次后放行防卡死。  
- **Web**：`deny_high=True`（High 一律 Deny，无 Confirm 弹窗）；SSE 增加 `auth_decision` / `tool_visibility` / `completion_gate`。  
- 冒烟覆盖阶段推断、可见集、Evidence 拦截、Web deny_high；17.8 P1 / 13.6 P1 可见集已勾选。  
- **下一步：** 交付录视频；有余力做 17.8 P2。

### 2026-08-30（三十四）安全权限 P0 落地（risk 元数据 + 敏感路径）

- **`FunctionTool`**：`risk_level` / `is_readonly`；各工具注册时填表（读类 low、写/shell medium）。  
- **`PermissionGate`**：`bind_registry` 读元数据；`assess_risk` 参数升级；`.env` / 密钥 / `.ssh` **始终 Deny**；High shell（`git reset --hard` 等）+ hard-deny；Low 在 ask/never 下仍放行。  
- 冒烟覆盖：读 `.env` Deny、`git reset --hard` high、`pytest` medium Allow、元数据断言。  
- README / 面试第 5 问 / 第 13·17 节现状表与 17.8 P0 勾选已更新。  
- **下一步：** 交付录视频；有余力做 17.8 P1（可见工具集 + Completion Evidence）。

### 2026-08-30（三十三）计划书写入「安全 / 权限 / Verification」

- 新增 **第 17 节**：问题定义（OWASP Excessive Agency / ASI02）；对照现有 `PermissionGate`；定案 **Permission → Risk → Allow/Confirm/Deny** 管线与 **三级风险表**；精读 [AgenTRIM](https://arxiv.org/abs/2601.12449)、[Towards Verifiably Safe Tool Use](https://arxiv.org/abs/2601.08012)（ICSE 2026）并映射到本仓库；补 **Completion Criteria / Evidence Gate**（与 16 节 Stop Condition 衔接）；P0–P2 改造清单。  
- 文首索引、面试第 5 问已同步。  
- **下一步：** 交付录视频优先；有余力从 17.8 P0（Registry `risk_level` + 敏感路径 Deny）开始实现。

### 2026-08-30（三十二）放宽 goal_met_forced（续改可用）

- **根因：** todo 勾完就催 FINAL + 再 mutate 强制停；续写还继承 `final_nudge_sent`。  
- **修复：** 每轮新用户消息 `clear_nudge_state`；**仅 `tests_all_pass` 可 `goal_met_forced`**；`todo_all_done` 只软提醒并可继续改。  
- **下一步：** 重启 Web/CLI 后再试「改完再继续改」的计时器类任务。

### 2026-08-30（三十一）无效行动护栏 P1 落地

- **RetryPolicy**（`src/agent/retry_policy.py`）：`failure_key=tool|path|error`；stage1 记录 → stage2 强制换策 → stage3 `retry_exhausted`；`RETRY_MAX_FAILURES`（默认 3）。  
- **Stop conditions**（`src/agent/stop_conditions.py`）：`tests_all_pass` / `todo_all_done` → 注入 `[stop_condition]` FINAL nudge；之后仍 mutating ≥`FINAL_NUDGE_MUTATING_LIMIT` → `goal_met_forced`。  
- Failed strategies 注入 Current State / TaskState；SSE：`retry_stage` / `final_nudge` / `final_nudge_warning`。  
- 冒烟覆盖阶梯 retry、催 final、export `retry_policy`；第 16.10 P1、15.6 Todo 催 final、面试 1·12 已更新。  
- **下一步：** 交付录视频；有余力做 16.10 P2（cycle / soft-dedup）或 15.6 切片读/grep。

### 2026-08-30（三十）无效行动护栏 P0 落地

- **抽出** `src/agent/loop_guard.py`：fingerprint / 连续 streak / 同轮 dedup / error-streak；`LOOP_WARN_AFTER` / `LOOP_STOP_AFTER` / `LOOP_ERROR_NUDGE_AFTER`（config + `.env.example`）。  
- **同轮 Action Dedup**：同一步相同 fingerprint 不二次执行，复用结果并打 `[dedup]`；SSE `action_dedup`。  
- **Error-streak**：同 fingerprint 连续失败 ≥2 注入 `ERROR_STREAK` 换策文案；SSE `error_streak`。  
- **TaskState MVP**：`src/agent/task_state.py`（goal / relevant_files / last_error / test_status + pytest 粗解析）；注入 Current State；`export_memory` → working_memory；SSE `task_state`。  
- 冒烟覆盖 dedup / error-streak / TaskState / Current State 注入；第 16.10 P0 与面试第 12 问已勾选。  
- **下一步：** 交付录视频；有余力做 16.10 P1（阶梯 Retry + tests_all_pass 早停）。

### 2026-08-30（二十九）计划书写入「减少无效推理 / 无效行动」

- 新增 **第 16 节**：厘清与 Context / 步数效率的边界；综合 tool-loop-guard、OpenHands StuckDetector、Kimi tool-dedup、Termination 四分类等；定案 **Action Dedup + Task State + Retry Policy + 分层终止**；P0–P2 清单。  
- 文首索引、第 15 节关系表、面试预案第 12 问已同步。  
- **下一步：** 交付录视频优先；有余力从 16.10 P0（同轮去重 / TaskState MVP / error-streak）开始实现。

### 2026-08-30（三十三）Web 成本面板

- 顶栏：`本轮 steps` + `≈ used/budget tok`（复用 `step_start` / `context_usage`）；悬停可见峰值。  
- FINAL 脚注带步数与峰值粗估；非 API 账单。  
- **下一步：** 录视频交付。

### 2026-08-30（三十二）运行中插话纠偏（Steer）

- `SteerInbox` + `POST /api/steer`；下一步边界注入 `[user_steer]`，并刷新 goal / 清 nudge。  
- Web：运行中输入框可继续发；发送 = 纠偏（不新开一轮）；Stop 仍在。  
- 冒烟覆盖 inbox drain。  
- **下一步：** 录视频交付。

### 2026-08-30（三十一）grep + read_file 切片

- `read_file`：可选 `offset`/`limit`（1-based 行）；≥200 行整读时提示切片。  
- 新工具 `grep`：workdir 内正则，`path:line:text`，封顶 matches/files。  
- system / explore 可见集 / compress / README / 冒烟已同步。  
- **下一步：** 录视频交付。

### 2026-08-30（三十）Web Stop + 协作取消

- P0：`threading.Event` 协作取消；`POST /api/stop`；运行中显示红色「停止」按钮。  
- 每步 / LLM 返回后 / 每个工具前检查；等待授权时 Stop 可立即解开。  
- 中断文案用户可读，并列出本轮已改文件；会话保留，可发下一条纠偏。  
- 冒烟：预置 cancel → `interrupted`；ApprovalBridge cancel。  
- **你需要：** 重启 Web + Ctrl+F5，用长任务点停止验证。

### 2026-08-30（二十九）Skill P1′：三 Skill + 关键词预注入

- 新增 `skills/testing`、`skills/refactoring`；`debugging` description 互斥收紧。  
- `suggest_skills` 关键词路由：高置信 Top-1 预注入正文（省 `load_skill` 步）；近平局不注入。  
- SSE / timeline：`skill_loaded`（via=keyword_router | load_skill）。  
- 冒烟覆盖三 Skill catalog / router / preload；README / 第 14.2·14.6 / 面试第 10 问已更新。  
- **下一步：** 录视频交付；有余力再做 references（L3）。

### 2026-08-30（二十八）提高上下文预算默认值

- `CONTEXT_TOKEN_BUDGET` 默认 **8000 → 32000**（config / ContextManager / `.env.example` / README）。圆环更不易「一轮就满」。  
- 若本地 `.env` 仍写着 `8000`，改掉或删掉该行后重启 Web/CLI。

### 2026-08-30（二十七）上下文圆环按轮记录 / 切换刷新

- 每轮结束把 `context_usage` 写入 `memory`、session `turns[]` 与顶层字段。  
- Web：切历史 / New task / 同会话新一轮会 `reset`；回放用该会话最后一轮（或缺省时按 messages 估算）。  
- 静态资源 `?v=20260830b`；重启 Web + Ctrl+F5。

### 2026-08-30（二十六）修复 tool 消息配对 API 400

- 根因：fold / trim / 续写瘦身可能留下孤立 `role=tool`（前面没有带 `tool_calls` 的 assistant）。  
- 修复：`sanitize_tool_pairing` + `expand_tail_for_tool_pairing`；接入 `prepare_messages` / `_fold_history` / `trim_messages` / `build_continue_context` / `run_agent` 入口。  
- 冒烟覆盖孤儿 tool / 半截 tool_calls。重启 Web/CLI 后再跑即可。

### 2026-08-30（二十五）步数效率 P0 落地

- **批工具 + 粗 Todo**：system 规则 #7/#8；`todo_write` description；debugging Skill / Web 建议 prompt / `demos/DEMO.md` 同步。  
- **循环检测**：`LoopGuard` + `tool_call_fingerprint`（≥3 警告注入结果，≥5 `loop_detected`）；SSE `loop_warning`。  
- 冒烟覆盖 fingerprint / streak / prompt 关键词；第 4.3 / 13.6 / 15.6 / 面试 1·9·11 已勾选更新。  
- **下一步：** 重启 Web / CLI 验证 greeter；对比 steps；交付录视频；有余力做 15.6 P1。

### 2026-08-30（二十四）计划书写入「步数 / Token 效率」改造路线

- 新增 **第 15 节**：诊断「一步任务多轮推理」根因（LLM 往返 ≠ 上下文压缩）；对照 Cursor/Claude Code 常见做法；本仓库 P0–P2（批工具 + 粗 todo + 切片读 / 循环检测 / 可选跳过计划）。  
- 文首索引与面试预案第 11 问已同步。  
- **下一步：** 交付录视频优先；有余力先做 15.6 P0（prompt 批工具 + 粗 todo + 立即清输入等体验已部分落地）。

### 2026-08-30（二十三）上下文容量条 + 回合自动总结

- **输入区容量条**：每步 / 续写时推送 `context_usage`（剩余%、level=ok/warn/critical）；Web 底部显示「上下文剩余 xx%」。  
- **回合结束自动总结**：写入 `MEMORY.md` + `.agent/last_turn_summary.md`，并 SSE `turn_summary` 在聊天区展示；session turn 存 `summary`。  
- 冒烟通过。重启 Web + Ctrl+F5 即可用。

### 2026-08-30（二十二）修复「继续做」续写跑偏

- 根因：续写把会话**第一个任务**当目标，且 recent_k 装不下上一轮 20 步，模型把「继续做」理解成重做 hello.py。  
- 修复：`last_active_task` / `resolve_continue_task`（展开「继续做」→ 未完成的 max_steps 任务）；`build_continue_context` 注入 ACTIVE GOAL briefing，并保证最近真实用户目标进窗口。  
- 冒烟覆盖续写解析。  
- **你需要：** 重启 Web，在同一会话再发「继续做」（或从左侧点开该 session 历史后再发）。

### 2026-08-30（二十一）修复 Windows shell GBK 解码 + max_steps

- `run_shell`：改 bytes 捕获 + utf-8/gbk 容错解码，并设 `PYTHONIOENCODING=utf-8`，消除 `UnicodeDecodeError: 'gbk'`。  
- 默认 `max_steps` 提到 30（config / Web / `.env.example`），减少「修 bug 中途撞上限」。  
- 冒烟 + `scripts/check_shell_encoding.py` 通过。  
- **你需要：** 重启 `python -m src.web`（或 CLI），浏览器 Ctrl+F5；Max steps 可再调到 40。

### 2026-08-30（二十）Skill P0 落地

- 新增 `skills/debugging/SKILL.md`；`src/agent/skills.py`（扫描 / L1 catalog / 读正文）；工具 `load_skill`；system 注入 Available Skills。  
- 冒烟覆盖 catalog + `load_skill`；README / 第 14.2 / 14.6 P0 / 面试第 10 问已更新。  
- **下一步：** 录视频交付；有余力再做 Skill P1（testing 等）或 Tool 循环检测。

### 2026-08-30（十九）计划书写入「Skill」改造路线

- 新增 **第 14 节**：Tool vs Skill vs Todo；Anthropic Progressive Disclosure；SkillReducer / SkillRouter 取长补短；本仓库 3～5 个 Skill 的落地设计与 P0–P2。  
- 文首索引与面试预案第 10 问已同步。  
- **下一步：** 先录视频交付；有余力再做 14.6 P0（`skills/` + 目录注入 + debugging 一个 Skill）。

### 2026-08-30（十八）计划书写入「Tool 编排」改造路线

- 新增 **第 13 节**：ReAct / Plan-Execute / 动态选工具 / Claude Code Registry 元数据等多方案取长补短；对照本仓库现状；P0–P2 改造清单。  
- 文首索引与面试预案第 9 问已同步。  
- **下一步：** 交付录视频；交付后按 13.6 优先做 P0（Tool 元数据 + 循环检测 + description/prompt 引导）。

### 2026-08-30（十七）Day4：README.txt + push

- 新增提交用 `README.txt`（约 395 汉字，含仓库地址、运行步骤、Todo 特色、能力与设计要点）。  
- P1/P2 代码已 push（`c69dacd`）；本条再提交 README.txt 与计划书勾选。  
- **下一步（你来做）：** 按 `demos/DEMO.md` 录 ≤2 分钟视频 → 打「姓名.zip」（仅 README.txt + 视频）提交。

### 2026-08-29（十六）长短期记忆 P2

- 本地 TF–IDF `rag_search`（代码 / MEMORY / transcripts → `.agent/rag_index.json`）。  
- ACON-lite：失败对写入并收紧 `.agent/compress_guideline.json`（不蒸馏小模型）。  
- MicroCompact：stub / summary / full；超 75% budget 先 stub 旧 tool 再 fold。  
- `cache_policy`：prompt_cache_key 经 `extra_body` 下发；无厂商 compaction 时本地 fold fallback。  
- 冒烟 / README / 第 11.6 P2 勾选已更新。  
- **下一步：** 交付录视频 / README.txt / push。

### 2026-08-29（十五）长短期记忆 P1

- Prompt 布局：system 单次 run 内缓存（稳定前缀）；Current State 改为消息**末尾可变后缀**。  
- 双轨：`workdir/.agent/working_memory.json`（+ transcripts 镜像）与 full transcript 分离；新 run 可 hydrate。  
- 新工具 `memory_search`：关键词搜 MEMORY.md + transcripts。  
- 折叠后重注 Root State（MEMORY + focus_files + 未完成 todo）。  
- 冒烟 / README / 第 11.6 P1 勾选已更新。  
- **下一步：** 交付录视频 / README.txt / push；P2（向量 RAG 等）可答辩提未做。

### 2026-08-29（十四）长短期记忆 P0

- 新增 `src/agent/memory.py`：`MEMORY.md` / `.agent/MEMORY.md` 开局注入、结束规则模板追加；模板 `MEMORY.md.example`，运行时文件 gitignore。  
- Context Manager：加载 Project Memory；todo → `completed` 时阶段压缩进 `history_summary`。  
- Web 续写：`build_continue_context`（memory 快照 + 最近 K + 原任务）；session 磁盘仍追加全量 messages。  
- 冒烟覆盖 MEMORY 读写 / 阶段压缩 / 续写瘦身；README + 第 11.6 P0 勾选。  
- **下一步：** 交付录视频 / README.txt / push；有余力再做 P1（前缀缓存友好 / `memory_search`）。

### 2026-08-29（十三）计划书写入「长短期记忆改造路线」

- 新增 **第 11 节**：业界分层、MemGPT/Claude Compact/ACON/RAG 结论、TUI/缓存含义、P0–P2 改造清单与落点文件。  
- 原「进度日志」改为第 12 节；面试预案补第 8 问。  
- **下一步：** 交付录视频 / README.txt / push；有余力再做第 11.6 节 P0（MEMORY.md）。

### 2026-08-29（十二）ACON 简化版 Context Manager

- 参考 [ACON](https://arxiv.org/abs/2510.00615) / [microsoft/acon](https://github.com/microsoft/acon)：不做失败对优化循环，落地工程版压缩器。  
- 新增 `src/agent/compress.py`（pytest/shell 观测压缩）+ `src/agent/context_manager.py`（五层：System / Task / Current State / Recent / Historical）。  
- Loop：工具结果先压缩再入 messages；超 `CONTEXT_TOKEN_BUDGET` 折叠中间轮为摘要；transcript 写入 `memory`。  
- 冒烟覆盖压缩与 prepare_messages；计划书 / README / 面试答「上下文爆了」已更新。  
- **下一步：** 录视频 → README.txt → push。

### 2026-08-29（十一）Web UI：侧栏固定 / Open folder / Files / Diff / Session 历史

- 左侧栏 `sticky` 不随主区滚动；**Recent chats** 独立滚动条。  
- **一大轮对话 = 一条 session 历史**（`session_*.json`）：同会话多轮续写会带上 prior messages，可供后续长短期记忆处理。  
- 顶部 **Open folder**：浏览本地目录并设为 workdir；右侧 **Files** 树可展开/折叠，点击文件新窗口预览。  
- `write_file` / `edit_file` 发出 `file_change` 事件；底部 **Changed files**，点击弹窗对比新旧内容。  
- 离线冒烟 `smoke_v1` 通过。  
- **下一步（交付）：** 录视频 → 写 README.txt → push → 打 zip。

### 2026-08-29（十）计划书「已完成总览」+ UI 富文本/折叠

- 计划书新增 **第 0 节「已完成总览」**：对照题目硬性要求、能力清单、未完成交付项、速查命令。  
- Web：Markdown 富文本；完成步骤自动折叠；进行中步骤滚到视口中央并高亮。  
- **下一步（交付）：** 录视频 → 写 README.txt → push 最新代码 → 打 zip 提交。

### 2026-08-29（九）结构化事件 + UI 步骤卡片

- `run_agent` 增加 `on_event`：`step_start` / `tool_call` / `tool_result` / `todo_update` / `final` 等。  
- Web：SSE 推送结构化事件；单任务锁；`GET /api/history/{id}` 回看；`POST /api/demos/reset` 恢复演示 bug。  
- 前端：步骤卡片、Todo 面板、Running 状态、历史回放；静态资源 `?v=` 防缓存。  
- **下一步：** 浏览器硬刷新 http://127.0.0.1:7860 试跑 greeter；录视频；Day4 提交物。

### 2026-08-29（八）Web UI

- 新增 `src/web/`：FastAPI + 静态前端（侧栏 / 问候 / 快捷卡片 / 底部输入，风格参考现代 AI 聊天 UI）。  
- `/api/run` 以 SSE 推送 tool 日志；底层复用 `run_coding_task`（同一套自研 harness，非套壳第三方 agent）。  
- 启动：`python -m src.web` → http://127.0.0.1:7860  
- **下一步：** 本地打开界面试跑演示任务；录视频；Day4 写 README.txt / 提交；可选 push GitHub。

### 2026-08-29（七）Day3 Todo 特色 + 演示定稿

- 实现 `todo_write`（`src/tools/todo.py`）+ system prompt「先计划后执行」约束。  
- 演示：`demos/greeter.py` + `greeter_test.py` + `DEMO.md`；README 补架构说明。  
- 端到端 5 步通过（todo → 读 → edit → 测试 ok → todo 完成）；源码已恢复为有意 bug。  
- **下一步（你来做）：** 按 `demos/DEMO.md` 录 ≤2 分钟视频；然后 Day4 写 README.txt、打 zip 提交。可选：把 Day2/Day3 代码 push 到 GitHub。

### 2026-08-29（六）Day2 Harness 合格线

- 新增：`edit_file`、`glob`；`PermissionGate.authorize`（hard-deny + auto/ask/never）；`trim_messages`；`save_transcript`。  
- CLI：`--approval` / `--max-messages` / `--transcript-dir`。  
- 离线冒烟通过；端到端修复 `buggy_calc.py` 通过（演示文件已恢复为有意 bug，便于复测）。  
- **下一步：** Day3 — 选定并实现一个特色（建议 Todo 或测试驱动修复）+ 定稿演示任务。

### 2026-08-29（五）推送到 GitHub

- 本地 `git init`，首次提交 `5faabcb`（不含 `.env`、不含考核 PDF）。  
- 已推送到公开仓库：https://github.com/hyj321/coding-agent （`main`）。  
- **下一步：** Day2 — `edit_file`、上下文裁剪、approval、transcript。

### 2026-08-29（四）V1 端到端验收通过

- Key 写在了 `.env.example`，已复制到 `.env`，并清空 example 中的密钥，避免误提交。  
- 真实任务：在 `demos/` 创建 `hello.py` 并 `python` 运行 → **3 步完成**（write_file → run_shell → 最终回复）。  
- **下一步：** Day2 — `edit_file`、上下文裁剪、approval、transcript。

### 2026-08-29（三）V1 代码落地

- 初始化项目骨架：`src/`（config / agent / llm / tools）、`requirements.txt`、`.env.example`、`.gitignore`、`README.md`。  
- 实现：`ToolRegistry`、`read_file` / `write_file` / `list_dir` / `run_shell`、路径沙箱、`LLMClient`、`run_agent` 主循环、CLI。  
- 扩展点：新工具只需注册；Permissions / Context 独立模块，便于 Day2 加 approval、裁剪、transcript。  
- 离线冒烟 `python -m scripts.smoke_v1`：**通过**。  
- **下一步：** 你配置 `DEEPSEEK_API_KEY` 后跑真实任务验收 → 通过后进入 Day2（`edit_file`、上下文裁剪、approval、transcript）。

### 2026-08-29（二）调研更新

- 调研并对比：ai-coding-agent、CoreCoder、mini-coding-agent、learn-claude-code、FutureAGI harness 文、Edd Mann 实践文等。  
- 结论写入本计划：以 **六层 harness** 叙事；开发顺序对齐 **v1 → 稳定 → 单特色**；明确「学架构不抄仓」。  
- 特色候选调整为更贴开源共识的 **Todo 规划 / 测试驱动修复 / 循环检测**。  
- 技术倾向：Python + DeepSeek tool calling。  

### 2026-08-29（一）初稿

- 完成计划书初稿，明确组件、必备/扩展功能与四天节奏。

---

## 13. Tool 编排：调研结论与后续改造清单

> 调研日期：2026-08-30。用途：交付后或有余力时按 P0→P1→P2 改；**Day4 交付前勿再开大坑**。  
> 心智模型：**编排 =「何时暴露哪些工具 + 何时允许副作用 + 如何根据观测改下一步」**，不是把业务写成死脚本。

### 13.1 问题定义：Tool 是什么、编排要解决什么

```text
Tool = Agent 可以实际执行的动作（副作用在本地，意图由模型发出）

本项目已有：
  read_file / write_file / edit_file / list_dir / glob
  run_shell / todo_write
  memory_search / rag_search
```

**编排要回答的不是「有没有工具」**（Registry 已解决），而是：

1. **选什么**：这一步该 `glob`/`grep` 还是盲目 `list_dir` + 连环 `read_file`？  
2. **何时调**：纯闲聊是否还要带全套 tools？修 bug 是否应先搜错误再读文件？  
3. **允不允许**：高风险命令是否要 ask / deny？（Permissions 层，与「选型」正交）  
4. **调完怎么办**：Observation 如何进 messages，失败如何让模型改道？（Loop 不变量）

**反例（坏路径）与正例（好路径）：**

| 用户任务 | 坏编排 | 好编排 |
|----------|--------|--------|
| 「修复这个 Python 项目的 bug」 | `list_dir`×N → `read_file`(无关文件)×N | `glob`/`grep` 定位 → `read_file` 相关文件 → `edit_file` → `run_shell`(测试) → 更新 todo |
| 「谢谢」 | 仍跑完整 tool loop | gating：无工具或 `tool_choice=none` |
| 「改完跑测试」 | 只改不验 | 改后强制/引导跑测试；失败继续 ReAct |

### 13.2 本项目现状（对照）

| 能力 | 状态 | 落点 |
|------|------|------|
| Tool Registry（name / description / parameters + handler） | ✅ | `src/tools/base.py` `FunctionTool` / `ToolRegistry` |
| 原生 tool calling 主循环（≈ ReAct） | ✅ | `src/agent/loop.py` |
| 执行前权限（沙箱 + approval + hard-deny） | ✅ | `PermissionGate` |
| 外层计划（Todo / Plan-then-Act） | ✅ | `todo_write` + system prompt |
| 工具错误回传 / max_steps / 可观测事件 | ✅ | loop + transcript + Web SSE |
| Registry 元数据：`permission` / `risk_level` / `cost` / `is_readonly` | ✅ | `FunctionTool.risk_level` / `is_readonly`；Gate `bind_registry` |
| 动态收窄「本轮可见工具集」 | ❌ | 每步 `openai_tools()` 全量下发 |
| 循环检测（重复同一 tool+args） | ✅ | `LoopGuard`：3 警告 / 5 停 `loop_detected` |
| 并行 tool_calls 的并发安全标记 | ❌ | 串行执行即可；元数据可预留 |
| MCP / 上千工具检索 | 不做 | 题目与时间均不划算 |

结论：**编排骨架已有（ReAct loop + Registry + Todo + Permissions）**；缺口在「元数据驱动的策略」与「防傻调」加固，而不是换框架。

### 13.3 多方案调研（取长补短）

#### A. ReAct（基础循环）— 必读

- 论文：[ReAct: Synergizing Reasoning and Acting in Language Models](https://arxiv.org/abs/2210.03629)（Yao et al.）  
- 建议程度：⭐⭐⭐⭐⭐（理解 Agent Loop，不必照搬 Thought 文本格式）

```text
Reason  →  Action(tool)  →  Observation  →  Reason  →  …
```

- **现代落地**：原生 function calling 已隐式完成「Action」；模型内部推理 ≈ Reason；工具返回 ≈ Observation。不必强制模型输出 `Thought:` 字符串。  
- **对本项目**：`loop.py` 就是 ReAct；答辩用 ReAct 叙事即可。  
- **短板**：每步都见全工具表；长任务易迷路、易重复调用；无显式全局计划时会「局部最优」。

#### B. Plan-and-Execute（外层计划 + 内层执行）

- 代表：[Plan-and-Execute Agents](https://blog.langchain.com/planning-agents/)；访谈叙事见 [ReAct vs Plan-and-Execute](https://buildingagenticai.com/blog/react-vs-plan-and-execute/)  
- 结构：Planner 先拆子目标 → Executor（常为小型 ReAct）逐步执行 → 失败可 Replan。  
- **生产常见组合**：**外层 Plan，内层 ReAct**（我们的 `todo_write` 正是轻量版）。  
- **取长：** 修 bug 这类多步任务先有「搜→读→改→测」骨架，减少乱读文件。  
- **补短：** 纯 Plan 不懂中间观测 → 必须保留 ReAct 内环；我们已用 Todo 勾选闭环。

#### C. 工具选择扩展（工具一多才需要）

业界共识：扁平暴露工具 **超过约 15–20 个** 后，选错工具 / 幻觉参数明显上升（见 [MLM Tool Selection Guide](https://machinelearningmastery.com/the-complete-guide-to-tool-selection-in-ai-agents/)、[Allen Chan](https://achan2013.medium.com/how-many-tools-functions-can-an-ai-agent-has-21e0a82b7847)）。六层手段（由浅入深）：

| 手段 | 做什么 | 我们要不要 |
|------|--------|------------|
| **Gating** | 先判断本轮要不要工具 | P2：闲聊 / 纯解释可关 tools |
| **Retrieval** | 按 query 检索 top-K 工具 schema（RAG-MCP / Dynamic ReAct） | 工具 <15 时 ROI 低；预留接口即可 |
| **Routing** | 先选类别（fs / shell / memory）再暴露子集 | P1：可按 Todo 阶段或任务类型收窄 |
| **Planning 定域** | 计划步带 `required_capability`，每步只挂相关工具 | 与 Todo 标签结合最自然 |
| **Fallback** | 低置信 → 改写 query / 澄清，不硬猜 | 工具少时靠模型重试即可 |
| **Benchmark** | 固定任务测「选对工具率」 | 答辩可用 demos 冒烟替代 |

- Dynamic ReAct / MCP 大规模选工具：[arxiv 2509.20386](https://arxiv.org/abs/2509.20386)；meta-tool「先搜工具再加载」。  
- **对本项目：** 当前工具数量仍在舒适区，**优先把 description + 策略 prompt 写好**，再谈向量选工具。

#### D. Claude Code 风格：富元数据 Registry + 执行管线

参考：[Inside Claude Code — Tool System](https://y-agent.github.io/inside-claude-code/05-tool-system.html)、[Claude Code from Source ch06](https://claude-code-from-source.com/ch06-tools/)

推荐 Tool 对象字段（在 name/description/parameters 之外）：

```text
Tool
├── name
├── description          # 给模型看：何时用 / 何时不要用
├── parameters           # JSON Schema
├── permission           # readonly | workspace_write | shell | network …
├── risk_level           # low | medium | high
├── cost                 # 相对代价：tokens / 时延 / 副作用（可枚举）
├── is_concurrency_safe  # 能否与同批其它 tool 并行
└── handler              # 本地执行
```

执行管线（我们已有简化版）：

```text
model tool_call
  → 校验参数 JSON
  → Permission / approval（按 risk_level）
  → dispatch(handler)
  → 截断/压缩 Observation
  → append messages
```

- **取长：** 元数据集中，approval 不再 hardcode 工具名；并行时只并发 `is_concurrency_safe` 的只读工具。  
- **对我们：** P0 把 `risk_level` / `permission` 挂进 `FunctionTool`，`PermissionGate` 读元数据。

#### E. 厂商协议层（OpenAI / Anthropic 共性）

- 模型只发意图；**Harness 执行或拒绝**；每个 call 必须有且仅有一条 result（含错误）。  
- 支持 `tool_choice`：`auto` / `none` / `required` / 强制某一工具。  
- 并行 tool_calls：只读可并行，写/ shell 宜串行。  
- 参考：[agentic-loop 不变量](https://github.com/DenisSergeevitch/agents-best-practices/blob/HEAD/references/agentic-loop.md)、[Function Calling 2026 综述](https://ofox.ai/blog/function-calling-tool-use-complete-guide-2026/)

### 13.4 合成：我们采用的编排架构（推荐叙事）

```text
                    ┌─────────────────────────────┐
  用户任务 ────────►│ 外层：Todo / Plan-then-Act   │  ← 已有
                    │ （目标分解 + 进度可见）       │
                    └─────────────┬───────────────┘
                                  │ 每步
                    ┌─────────────▼───────────────┐
                    │ 内层：ReAct + native tools   │  ← 已有
                    │ Reason → tool_calls → Obs   │
                    └─────────────┬───────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
        Tool Registry      PermissionGate      Context/Compress
        （schema+元数据）   （risk→ask/deny）    （观测可消化）
              │                   │
              └─────────┬─────────┘
                        ▼
                   本地 handler
```

**设计原则（面试可背）：**

1. **模型选工具，Harness 控边界** — 不手写「if bug then grep」。  
2. **全量工具仅在小目录时合理** — 超过 ~15 再上 routing/retrieval。  
3. **用计划与 description 引导「好路径」** — 搜 → 读 → 改 → 测，而不是 list 扫库。  
4. **风险进元数据** — 同一套 Gate，新增工具只填 `risk_level`。  
5. **Observation 必须回传** — 错误也是编排信号。

### 13.5 Coding Agent 的推荐工具策略（写进 prompt / description）

| 阶段 | 优先工具 | 避免 |
|------|----------|------|
| 定位 | `glob`、（可选 `grep`）、`memory_search` | 无目标 `list_dir` 递归、乱读大文件 |
| 理解 | `read_file`（定点） | 一次读整个仓库 |
| 修改 | `edit_file` | 无必要整文件 `write_file` |
| 验证 | `run_shell`（测试/复现） | 改完直接宣称成功 |
| 规划 | `todo_write` 开局 + 完成即更新 | 无 Todo 长任务裸奔 |

System / 工具 description 应显式写出「Prefer search before broad listing」类约束（成本接近零、收益高）。

### 13.6 后续改造清单

#### P0 — 性价比最高（交付后优先）

- [x] **扩展 `FunctionTool` 元数据**  
  - 字段：`risk_level` / `is_readonly`（`cost` / concurrency 未做）  
  - `PermissionGate.authorize` 优先读元数据，少写死工具名  
  - 落点：`src/tools/base.py` + 各 `register_*` + `gate.bind_registry`（与第 17.8 P0 合并）  
- [x] **循环检测（特色 C）**  
  - 连续相同 `(name, args_fingerprint)` → ≥3 注入 Observation 警告；≥5 `stopped_reason=loop_detected`  
  - 落点：`src/agent/loop.py` `LoopGuard` / `tool_call_fingerprint`  
- [x] **强化 description + system 编排指引**  
  - 同轮批工具；粗阶段 todo；修 bug 先搜后读；禁止纯记账空步（与第 15.6 P0 合并） 

#### P1 — 体验与可控性

- [x] **按阶段 / 任务类型收窄可见工具**（轻量 routing，不做向量）  
  - 例：explore 隐藏写/shell；verify 保留 `run_shell`+`edit_file`；信号弱时 `full`  
  - `registry.openai_tools(names=...)` + `TOOL_VISIBILITY=auto`（与第 17.8 P1 合并）  
- [ ] **`tool_choice` 策略钩子**  
  - 开局可 `required`/`todo_write`（慎用）；纯问答 `none`  
- [ ] **并行只读**：同轮多个 `read_file`/`glob` 可并发；写与 shell 串行  
- [ ] **补 `grep` 工具**（若仍缺）：定位类任务显著优于连环 read  

#### P2 — 重 / 可答辩提「下一步」

- [ ] **工具检索（RAG over tool descriptions）** — 仅当工具数明显增长或接 MCP 时  
- [ ] **meta-tool `search_tools` / `load_tools`**（Dynamic ReAct）  
- [ ] **编排评测集**：固定「修 greeter / buggy_calc」统计工具序列是否符合好路径  
- [ ] **MCP** — 默认不做（题目 + 复杂度）

### 13.7 精读顺序（改代码前 1–2 小时）

1. ReAct 论文摘要 / [IBM: What is a ReAct Agent](https://www.ibm.com/think/topics/react-agent) — 建立循环直觉  
2. [Plan-and-Execute](https://blog.langchain.com/planning-agents/) + 对照我们的 Todo  
3. [Tool Selection Guide（gating→retrieval→routing→planning）](https://machinelearningmastery.com/the-complete-guide-to-tool-selection-in-ai-agents/) — 知道「工具一多怎么办」  
4. [Claude Code Tool System](https://y-agent.github.io/inside-claude-code/05-tool-system.html) — 元数据与执行管线  
5. （可选）[Dynamic ReAct](https://arxiv.org/abs/2509.20386) — 大规模工具时再读  

### 13.8 改造时注意（防踩坑）

1. **交付期**：与第 11 节相同，Day4 不开编排大坑；先录视频交作业。  
2. **勿用框架**：编排逻辑写在 `loop` / `tools` / `permissions`，不要引入 LangGraph/CrewAI。  
3. **答辩一句话**：内环 ReAct（原生 tool calling）决定「下一步动作」；外环 Todo 决定「整体不跑偏」；Registry 元数据 + Permission 决定「能不能真执行」。  
4. **改完后**：更新本节勾选、面试第 9 问、冒烟加「循环检测 / 元数据 approval」、进度日志追加一条。

---

## 14. Skill（任务方法包）：调研结论与后续改造清单

> 调研日期：2026-08-30。用途：交付后或有余力时按 P0→P1→P2 改；**Day4 交付前勿再开大坑**。  
> 心智模型：**Tool = 能力；Skill = 完成一类任务的方法；Todo = 这一次任务的运行时计划。**

### 14.1 问题定义：三者别混

| 概念 | 是什么 | 生命周期 | 例子 |
|------|--------|----------|------|
| **Tool** | 原子动作 / 副作用入口 | 常驻 Registry，每步可调 | `read_file` / `edit_file` / `run_shell` |
| **Skill** | 可复用的程序化知识（步骤、约束、检查清单） | 目录常驻；**正文按需加载** | `debugging` / `testing` / `refactoring` |
| **Todo** | 当前任务的具体计划实例 | 单次 run 内可变 | 「1.读测试 2.改 greeter 3.跑测」 |

```text
Tools:   read_file, write_file, edit_file, glob, run_shell, todo_write, …
Skills:  debugging, testing, refactoring, code_review, （可选）git_commit

例如 debugging Skill 的「方法」：
  1. 分析错误信息 / 失败断言
  2. 定位相关文件（glob / grep，勿盲目 list）
  3. 读相关代码与测试
  4. 最小修改（优先 edit）
  5. 运行测试
  6. 失败则回到 2；成功则更新 todo / 收尾
```

**为什么要 Skill：** Agent 不必每次从零想「修 bug 先干什么」——把成功路径固化成可注入的短剧本，减少瞎读文件、改完不测等坏路径。

**和 Todo 的关系（答辩必说清）：**

- Skill 告诉模型「这类任务通常怎么走」；  
- Todo 把「这一次」落成可勾选步骤；  
- 好实现：命中 `debugging` → 注入 Skill 正文 → 模型用 `todo_write` 实例化计划 → ReAct 调 Tool 执行。

**和第 2.3 节「不做 Skills 生态」的关系：** 那里指的是 Claude Code 级插件市场 / 子 agent Skills 体系。本节做的是 **3～5 个本地 Markdown Skill + 渐进披露**，体量可控、可答辩。

### 14.2 本项目现状（对照）

| 能力 | 状态 | 说明 |
|------|------|------|
| Tool Registry + ReAct loop | ✅ | 有能力，缺「方法包」 |
| Todo / Plan-then-Act | ✅ | 运行时计划；非跨任务可复用方法 |
| system prompt 里的行为约束 | ✅ 部分 | 散装规则，难按任务类型切换 |
| `skills/` 目录 / SKILL.md | ✅ | `debugging` / `testing` / `refactoring` |
| Skill 目录（name+description）注入 | ✅ | `format_skills_catalog` → system L1 |
| 命中后加载正文 / references | ✅ 正文 / ❌ references | `load_skill`（L2）或关键词预注入；references 留余力 |
| Skill Router（检索/重排） | ✅ 轻量 | 关键词打分 Top-1 预注入；不做 embedding |

结论：**P0 + P1′ 已落地**（3 个互斥 Skill + L1 目录 + `load_skill` + 关键词预注入 + `skill_loaded`）。交付仍优先录视频。

### 14.3 多方案调研（取长补短）

#### A. Anthropic Agent Skills — 工程范式（必学）

- 文档：[Equipping agents for the real world with Agent Skills](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills)  
- 形态：每个 Skill = 一个目录 + `SKILL.md`（YAML frontmatter: `name` / `description`）+ 可选 `references/`、脚本。  
- **Progressive Disclosure（三级加载）**：

```text
L1 启动：只把各 Skill 的 name + description 放进 system（目录索引）
L2 命中：Agent 调 load_skill("debugging")（或 read_file SKILL.md）注入正文
L3 按需：正文指引再读 references/checklist.md、examples 等
```

- **取长：** 与「有文件系统的 coding agent」天然契合；上下文成本随任务涨，不随 Skill 库涨。  
- **对本项目：** **直接照这个目录约定落地**，不要发明复杂协议。

#### B. SkillReducer — 怎么写才不爆 token（⭐⭐⭐⭐⭐）

- 论文：[SkillReducer: Optimizing LLM Agent Skills for Token Efficiency](https://arxiv.org/abs/2603.29919)（Gao et al.；分析 55,315 公开 Skill）  
- 发现：约 26.4% 缺 routing description；超 60% body 不可执行；reference 可灌入上万 token。  
- 两阶段优化：  
  1. **Routing 层**：压缩/补全 description（可路由的短描述）  
  2. **Body 层**：分类（core rules / background / examples / templates / redundant）→ **只常驻 core**，其余 progressive disclosure  
- 报告：约 **48% description 压缩、39% body 压缩**，功能质量略升（less-is-more）。  
- **对本项目取长：**  
  - 每个 Skill description ≤ ~80 字，写清 **何时用 / 何时不用**；  
  - `SKILL.md` 正文只留 **可执行步骤**（调试六步），例子进 `references/`；  
  - **不做**论文里的自动 delta-debug 压缩流水线（过度工程）。

#### C. SkillRouter — 大规模怎么选 Skill（⭐⭐⭐⭐，我们降配）

- 论文：[SkillRouter: Retrieve-and-Rerank Skill Selection for LLM Agents at Scale](https://arxiv.org/abs/2603.22455)（~80K Skill）  
- 洞察：只靠 name+description 做路由，在**大规模+高度重叠**库上会掉很多点；body 对重排很关键。  
- 结构：`task → bi-encoder 检索 Top-K → cross-encoder 用 body 重排 → 最终 Skill`。  
- **对本项目：** 5 个 Skill **不需要** 1.2B 双塔。降配方案：

```text
5 个 Skill
  → 关键词 / 简单规则（或本地 TF–IDF，复用 rag 思路）
  → 选 1～2 个
  → 注入 L2 正文
```

- 若路由不准：把 description 写得更互斥（SkillReducer 的 routing 层思想），比上重排模型更划算。

#### D. 其他相关（知道即可）

| 思路 | 与 Skill 关系 | 我们怎么用 |
|------|---------------|------------|
| learn-claude-code v4 Skills | 教学路径终点 | 对齐「有余力再做」 |
| MCP | 外挂 Tool/资源协议 | ≠ Skill；Skill 教「怎么组合 Tool」 |
| 子 Agent | 隔离上下文执行子任务 | 比 Skill 重；本作业不做 |
| MEMORY.md | 项目事实 / 偏好 | Skill 是流程，Memory 是事实；可并存 |

### 14.4 合成：推荐架构（本仓库）

```text
启动
  └─ 扫描 skills/*/SKILL.md frontmatter
  └─ system 追加「Available Skills」目录（仅 name + description）   ← L1

用户任务进入 loop
  ├─（可选）SkillRouter 轻量：关键词打分 → 建议 1～2 个 Skill
  ├─ 或：让模型自行决定「先 read_file 某个 SKILL.md」         ← L2
  └─ Skill 正文可写：需要细节时再读 references/xxx.md         ← L3

执行
  └─ 仍走原 ReAct + Tools；Todo 实例化步骤；Permission 不变
```

**推荐目录（落地时）：**

```text
skills/
  debugging/
    SKILL.md              # frontmatter + 核心步骤（短）
    references/
      failure_checklist.md
  testing/
    SKILL.md
  refactoring/
    SKILL.md
  code_review/
    SKILL.md
```

**首批 3～5 个 Skill 选题（贴演示）：**

| Skill | 触发描述要点 | 核心步骤（正文） |
|-------|--------------|------------------|
| `debugging` | 修 bug / 测试失败 / traceback | 析错→定位→读码与测→最小改→跑测→循环 |
| `testing` | 补测 / 跑通测试套件 | 读现有测→写/改测→跑→修红 |
| `refactoring` | 重构但不改行为 | 先测保底→小步改→每步跑测 |
| `code_review` | 审阅改动 | 列风险→读 diff 相关文件→给可执行建议（少空谈） |
| （可选）`explore` | 陌生仓库摸底 | 顶层结构→入口/测试→记 MEMORY，勿大改 |

### 14.5 与现有模块怎么接

| 模块 | 接入点 |
|------|--------|
| `ContextManager` / system 组装 | L1：注入 Skills 目录；注意前缀稳定（利 cache） |
| `read_file` | L2/L3：Skill 正文与 reference 就是普通文件 |
| `todo_write` | Skill 要求「先按本 Skill 写 todo」 |
| `PermissionGate` | Skill **不**增加权限；危险仍走 Gate |
| Web / transcript | 可打事件 `skill_loaded`，视频里好看 |

**两种触发策略（选一种做主）：**

1. **模型自发现（更贴 Anthropic）**：目录在 system；模型自己 `read_file("skills/debugging/SKILL.md")`。实现最简单。  
2. **Harness 预路由（更好控演示）**：任务含「修复/测试失败」→ 自动把 debugging 正文塞进首轮 user/system 附加块。演示 greeter 更稳。

建议：**P0 用策略 1 + 清晰 description；演示任务 prompt 里点名「可参考 skills/debugging」**；P1 再加关键词预路由。

### 14.6 后续改造清单

#### P0 — 最小可答辩（约 0.5～1 天）

- [x] 建 `skills/` + 至少 **`debugging/SKILL.md`**（短正文 + 互斥 description）  
- [x] 启动扫描 frontmatter，system 注入 Available Skills（L1）  
- [x] 冒烟：`load_skill` + catalog 注入；演示可走「load_skill → todo → 修 → 测」  
- [x] README / 面试第 10 问勾选「已实现轻量 Skill」

#### P1 — 像模像样

- [x] 补齐 `testing` / `refactoring`（每个正文 < ~40 行可执行步骤；`code_review` 暂缓）  
- [ ] `references/` 按需加载（SkillReducer 分层）— 有余力再做  
- [x] 轻量 Router：关键词选 Top-1 预注入（近平局则不注入，交给模型 `load_skill`）  
- [x] timeline 事件 `skill_loaded`（预注入 + 模型调 `load_skill`）

#### P2 — 重 / 答辩提下一步

- [ ] 自动压缩 description（SkillReducer Stage1 简化版）  
- [ ] Embedding / 双塔 SkillRouter（仅当 Skill ≫ 20）  
- [ ] Agent 自写/自改 Skill（Anthropic「未来方向」；作业勿深挖）  
- [ ] Skill 与 MCP 工作流编排

### 14.7 时间建议（结合还剩约 3 天）

| 顺序 | 做什么 | 原因 |
|------|--------|------|
| 1 | **录视频 + zip 交付** | 硬门槛 |
| 2 | （可选）Skill **P0：仅 debugging** | 故事完整、改动面小于 Tool 元数据大重构 |
| 3 | Tool 循环检测 / prompt 引导 | 与 Skill 互补，可并行小改 |
| 4 | Skill P1 / Tool 元数据 / Router | 有余力再做 |

**要不要做 Skill？**  
- 只交作业：可不做。  
- 还想加一个「能讲论文与工程」的亮点：**做 P0 一个 debugging Skill 性价比最高**；不要一上来做 5 个 + 向量路由。

### 14.8 精读顺序（改代码前 1～2 小时）

1. [Anthropic: Agent Skills](https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills) — 三级 progressive disclosure  
2. [SkillReducer](https://arxiv.org/abs/2603.29919) — 摘要 + §发现与两阶段（重点看怎么写 Skill，不必复现优化器）  
3. [SkillRouter](https://arxiv.org/abs/2603.22455) — 只读动机与「我们降配」结论  
4. 对照本仓库 `todo_write` 与 demos 修 bug 路径，写第一份 `debugging/SKILL.md`

### 14.9 改造时注意（防踩坑）

1. **Skill 正文要短**：只写可执行步骤；例子进 references（SkillReducer less-is-more）。  
2. **description 要可路由**：写触发词与反例（「不要用于从零写新功能」）。  
3. **勿用框架 / 勿抄 Claude Skill 市场**；本地 Markdown + `read_file` 即可。  
4. **Skill ≠ 新权限**：不能让 Skill 绕过沙箱。  
5. **改完后**：更新本节勾选、面试第 10 问、冒烟、进度日志；视频若已录可口头补一句「我们还有 skills/debugging」。

---

## 15. 步数 / Token 效率：调研结论与后续改造清单

> 调研日期：2026-08-30。动机：真实任务常跑到十几～三十步，**慢且费 token**；交付后或有余力按 P0→P1→P2 改。  
> 心智模型：**贵的是「LLM 往返次数 × 每步窗口大小」**；已有 Context Manager 主要砍「每步窗口」，**几乎不砍「往返次数」**。要提速，必须同时治「步数膨胀」。

### 15.1 问题定义：慢在哪、贵在哪

```text
一次用户任务
  └─ for step in 1..max_steps:          ← 默认 MAX_STEPS=30
        prepare_messages (压缩/折叠)    ← 管 token，不管步数
        LLM chat()                      ← 主延迟 + 主费用
        串行执行本步全部 tool_calls
        观测压缩后写回 messages
```

| 成本维度 | 谁决定 | 本仓库现状 |
|----------|--------|------------|
| **步数**（LLM 往返） | 模型是否继续调工具；prompt / todo 粒度；是否重复读改 | 易膨胀；仅靠 `max_steps` 硬顶 |
| **每步 prompt token** | 历史长度 + 工具 schema + 观测体积 | ✅ 观测压缩 / MicroCompact / fold / 阶段压缩 |
| **每步 completion** | 模型输出长度 + tool_calls 个数 | 未专门约束 |
| **墙钟时间** | 步数 ×（LLM 延迟 + 工具 IO） | 工具串行；多 tool_calls 仍顺序 dispatch |

**常见「费步」路径（对照 demos / Web 真实跑法）：**

| 现象 | 典型浪费 | 根因 |
|------|----------|------|
| 先 `todo_write`  alone | +1 纯记账步 | Plan-then-Act 硬规则 + 模型不敢同轮干正事 |
| 每改一项再 `todo_write` | +N 记账步 | todo 过细 +「随时更新状态」 |
| `list_dir` → 逐文件 `read_file` | +大量探索步 | 缺 grep / 缺 offset·limit；prompt 虽写「读切片」工具做不到 |
| `edit_file` 匹配失败 → 再读再改 | +2～4 步 | 唯一匹配失败无兜底策略 |
| `load_skill` / `memory_search` / `rag_search` | +1～3 元步骤 | 有用但应可批进首轮 |
| 撞 `max_steps` 再「继续做」 | 再开一整轮预算 | 续写治目标，不治粒度 |

### 15.2 本项目现状（对照）

| 能力 | 状态 | 落点 | 对步数？ |
|------|------|------|----------|
| ReAct loop + 同一步多 `tool_calls` | ✅ | `loop.py`：一步内可多个 tool，**串行**执行 | 已支持并行**意图**，prompt 未强制 |
| Plan-then-Act / `todo_write` | ✅ | system #7 + `todo.py` | **故意加步**换可控 |
| 观测压缩 / fold / MicroCompact | ✅ | `compress.py` / `context_manager.py` | 只省 **token** |
| Todo 完成 → 阶段压缩 | ✅ | `_phase_compress_on_todo` | 只省 token |
| 续写瘦身 + ACTIVE GOAL | ✅ | `build_continue_context` | 续跑更准，不减步 |
| Skill L1/L2 | ✅ | `skills/` + `load_skill` | 可能 +1 步换少迷路 |
| Prompt「同一步批工具」 | ❌ | — | 高杠杆缺口 |
| 按任务难度跳过 / 粗化 Todo | ❌ | 规则对所有任务一刀切 | 高杠杆缺口 |
| `read_file` offset/limit 或 `grep` | ❌ | 整文件读 | 中高杠杆 |
| 循环检测（同 tool+args 重复） | ✅ | `LoopGuard` | 中杠杆 |
| Todo 全完成 → 催 final | ❌ | 只等模型自觉停 | 中杠杆 |
| 工具内并行（只读） | ❌ | 一律串行 | 低～中（墙钟） |
| 子 Agent / 委托探索 | ❌ | 计划书明确不做深挖 | 大仓才划算 |

结论：**压缩管线已较完整；下一步主战场是「减少 LLM 往返」与「减少探索型小步」**，不是再加一层摘要。

### 15.3 业界常见做法（取长补短）

#### A. 同轮批处理 Tool Calls（Batch / Parallel tools）— ⭐⭐⭐⭐⭐

- Cursor / Claude Code / OpenAI Agents：鼓励模型**一次响应里发多个 tool_calls**（读 A+B、或 edit+run_shell）。  
- **对本项目：** Loop **已经**支持；差在 **system / tool description 没写死「能批就批」**。  
- **取长：** 改 prompt 成本极低，步数可立降。  
- **补短：** 有依赖的调用仍应串行（先测后改依赖结果）；只读可并行意图。

#### B. 粗计划 + 少记账（Coarse Plan-then-Act）— ⭐⭐⭐⭐⭐

- 生产 agent 仍用 checklist，但条目是「定位失败 → 最小修复 → 跑测」级，**不是**「打开文件 1 / 打开文件 2」。  
- 状态更新：**阶段边界**再 `todo_write`（完成一项 / 改道时），禁止每工具后勾一次。  
- **对本项目：** 改规则 #7 + `todo_write` description + 演示文案；可选「trivial 任务跳过 todo」。

#### C. 探索工具升级（Grep / 切片读）— ⭐⭐⭐⭐

- Claude Code：`grep` / `Glob` + 按行范围读，避免整仓吞文件。  
- **对本项目：** `read_file` 加 `offset`/`limit`；或加轻量 `grep`（workdir 内）。  
- 与第 13 节「选对工具」互补：少 `list_dir` 连环。

#### D. 循环检测与早停 — ⭐⭐⭐⭐

- 开源 ai-coding-agent / 多款 harness：连续 N 次相同 tool+args → 注入警告或打断。  
- Todo 全 `completed` 且最近无失败 → 系统附加「请给出 FINAL，勿再调工具」。  
- **对本项目：** 落在 `loop.py`；与第 13.6 P0「循环检测」合并做。

#### E. Spec / 两阶段（先短计划再执行）— ⭐⭐⭐

- 部分产品：先廉价模型或短 completion 出大纲，再执行。  
- **对本项目：** 已有 Todo，不必再拆第二个 LLM；避免双倍往返。

#### F. 子 Agent / 并行 worker — ⭐⭐

- 大仓探索可委托只读子循环。  
- **对本项目：** 答辩可提「未做 / 下一步」；作业期内不做。

#### G. 只抬 max_steps / budget — ⭐（治标）

- 提高 `MAX_STEPS` 或 `CONTEXT_TOKEN_BUDGET`：**让任务跑得完或单步更胖**，**不会让步数变少**。  
- 用途：演示防撞顶；效率改造仍靠 A–D。

### 15.4 目标架构（效率视角）

```text
用户任务
  │
  ├─（可选）难度启发：trivial → 跳过强制 todo
  │
  └─ ReAct loop
        ├─ Prompt 约束：同一步多 tool；todo 仅阶段更新；探索用 glob/grep/切片
        ├─ 一步内 tool_calls：只读可标记并行（P1）；写/shell 仍串行+Gate
        ├─ 观测压缩（已有）→ 控制每步 token
        ├─ 循环检测：重复调用 → warn / stop
        └─ 早停：todos 全完成 + 无 pending 错误 → 催 final
```

**与第 11 / 13 / 14 节关系：**

| 节 | 管什么 | 与效率 |
|----|--------|--------|
| 11 记忆 | 窗口里留什么 | 降 **每步 token** |
| 13 Tool 编排 | 选什么工具、防傻调 | 降 **探索步 / 错步** |
| 14 Skill | 一类任务的先验剧本 | 少绕路；注意 `load_skill` 本身 +1 步 → 可与首轮批 |
| **15 本节** | **往返次数与粒度** | 降 **步数与墙钟** |
| **16 无效行动** | **防转圈 / 无进展** | 降 **重复步与失败空转**（与 15 互补） |

### 15.5 推荐策略（本仓库定案）

1. **P0 只动 prompt + 文案 + 小检测**：不改架构也能明显少步。  
2. **Todo 保留特色，但改粒度**：答辩仍能讲 Plan-then-Act；演示改「3～5 条阶段项」。  
3. **压缩管线维持**：效率改造不推翻 Context Manager。  
4. **交付期**：Day4 录视频优先；本节默认交付后实施。  
5. **度量**：同一 demos 任务对比 `steps`、估算 prompt tokens、墙钟；写进冒烟或手记。

### 15.6 后续改造清单

#### P0 — 低成本高回报（约 0.5 天）

- [x] **System / tool description：强制批工具**  
  - 明确：同一 assistant 消息可并行多个只读调用；`todo_write` 可与 `read_file`/`glob` 同轮；禁止「只记账不干活」的空步。  
  - 落点：`context_manager.build_system_prompt`、`todo.py` description  
- [x] **粗粒度 Todo 规则**  
  - 「非平凡任务」3～5 项阶段目标；平凡单文件改动可跳过 todo。  
  - 更新 demos / Web 建议 prompt，去掉「逐步勾每一小步」话术。  
- [x] **循环检测 MVP**  
  - 连续 ≥3 次相同 `(name, args_fingerprint)` → `on_event` 警告 + 注入 tool 结果提示；≥5 → `stopped_reason=loop_detected`。  
  - 与第 13.6 P0 合并完成。  
- [x] **度量基线**  
  - 固定任务说明见 `demos/DEMO.md`（改造前参考 5～15 步；对比 CLI `steps=` / transcript）。

#### P1 — 探索与早停（约 1 天）

- [x] **`read_file` 支持 `offset`/`limit`（行）**；超长文件默认提示「请切片读」  
- [x] **可选 `grep` 工具**（workdir 内正则，结果封顶）— 减少盲目 list/read  
- [x] **Todo 全完成催 final**：检测 todos 均 completed 且本步无 tool 失败 → 附加短指令要求文字收尾  
- [ ] **Skill 批加载**：prompt 允许 `load_skill` 与首次 `glob`/`read_file` 同轮  
- [ ] **只读 tool 并行 dispatch**（ThreadPool，写操作仍串行）— 主要省墙钟

#### P2 — 重 / 答辩提下一步

- [ ] 动态收窄可见工具集（按 Todo 阶段：探索期藏 `write_file` 等）— 接第 13 节  
- [ ] 失败 edit 自动建议「整段 rewrite / 放宽 match」减少重试步  
- [ ] 子 Agent 只读探索（大仓）；或二次 LLM 专责压缩（贵，慎用）  
- [x] 成本面板：Web 显示本回合 steps + 估算 tokens（延伸现有 context ring）

### 15.7 时间建议（结合交付）

| 顺序 | 做什么 | 原因 |
|------|--------|------|
| 1 | **录视频 + zip** | 硬门槛 |
| 2 | **15.6 P0（prompt 批工具 + 粗 todo + 循环检测）** | 改动面小、体感最大 |
| 3 | P1 切片读 / grep / 催 final | 修 bug 类任务继续降步 |
| 4 | P2 动态工具集 / 成本面板 | 有余力再做 |

### 15.8 改造时注意（防踩坑）

1. **少步 ≠ 砍 Todo 特色**：粗计划仍要可观察；视频里仍可展示 Plan 面板。  
2. **批工具时注意依赖**：先 `run_shell` 再根据输出修改，不能假装已并行。  
3. **循环检测误伤**：格式化/空白差异会导致「假不同」；fingerprint 先 normalize JSON key。  
4. **勿靠抬 max_steps 装快**：那只是延后失败。  
5. **改完后**：更新本节勾选、面试第 11 问、冒烟加「批工具 / 循环检测」用例、进度日志追加一条。

---

## 16. 减少无效推理 / 无效行动：调研结论与改造方案

> 调研日期：2026-08-30。动机：Agent 常「瞎转圈」——同一文件连读三遍、失败后原样重试、edit→test→失败→再同一招。  
> **与 Context（第 11 节）的关系：** Context 管「窗口里留什么、怎么压缩」；本节管「行为有没有前进、何时该停 / 换策」。压缩再好，也拦不住 `read_file(A)×3`。  
> **与步数效率（第 15 节）的关系：** 15 节降「正常路径上的步数」（批工具、粗 todo）；本节降「病理路径上的空转」（去重、状态机、阶梯重试）。二者互补，共享 `LoopGuard` 起点。

### 16.1 问题定义：什么叫「无效」

```text
有效推进：观测 → 新信息 / 状态变化 → 下一步与上一步不同且指向目标
无效行动：重复同一 (tool, args) / 同一失败策略 / 世界状态无变化仍继续烧步
无效推理：模型「以为」在试新方法，实际 fingerprint 或策略签名未变
```

| 典型转圈 | 表象 | 根因 | 仅靠 Context？ |
|----------|------|------|----------------|
| `read_file(A)` × N | 连续相同调用 | 模型不记得刚读过 / 不信观测 | ❌ 需 Action Dedup |
| edit → test 失败 → 原样再 edit | 策略签名相同 | 无「已失败策略」记忆 | ❌ 需 Retry Policy + Task State |
| A→B→A→B（读测交替） | 周期而非单点重复 | 只查 consecutive 会漏 | ❌ 需 cycle / stuck 检测 |
| 测试已全绿仍改文件 | 目标达成不感知 | 无可验证 Stop Condition | ❌ 需 Task State + 早停 |
| 撞 `max_steps` 才停 | 预算兜底太晚 | 缺 pathology 早停 | ❌ 需分层终止 |

**一句话：** 无效行动是 **控制面 / 终止面** 问题，不是 **记忆压缩** 问题。

### 16.2 业界做法（取长补短）

| 来源 | 机制 | 可借鉴 | 本仓库勿照搬 |
|------|------|--------|--------------|
| [tool-loop-guard](https://github.com/MukundaKatta/tool-loop-guard) | 滑动窗口内 `(tool, args)` 出现次数 ≥ threshold | 简单、零依赖、毫秒级 | — |
| [agent-loop-guard](https://github.com/ArkNill/agent-loop-guard) / [RunGuard](https://runguard.dev/seo/how-to-detect-llm-tool-call-loop-in-production.html) | Exact + Fuzzy + **Cycle(A→B→C)** + Output Stagnation；warn→stop→escalate | 覆盖 ping-pong / 短周期 | 模糊相似度可后做 |
| [Kimi Code tool-dedup](https://github.com/MoonshotAI/kimi-code) | 同轮去重复用结果；跨步 streak 3/5/8 分级提醒，12 强制停 | **分级提醒再硬停**（我们已有 3/5） | 阈值可调，不必照抄 12 |
| [OpenHands StuckDetector](https://docs.openhands.dev/sdk/guides/agent-stuck-detector) | 5 类：同 action-obs、同 action-error、独白、交替、上下文窗错误；先 nudge 再 stuck | **失败 streak 单独计**；可配置关闭 | 独白检测对本项目次要 |
| AWS Strands Debounce Hook | 执行前拦截重复，返回 BLOCKED 迫使换策 | 「不执行、只回拒」省 IO | — |
| [Towards AI: Anatomy of Termination](https://pub.towardsai.net/when-should-an-agent-stop-the-anatomy-of-termination-17644145309a) | 终止四类：Goal / Resource / **Pathology** / External；Retry 分 transient/format/semantic/**strategy** | 答辩框架 + Retry 分层 | 不必做成完整状态机框架 |
| [Agent Patterns: Stop Conditions](https://www.agentpatterns.tech/en/examples/stop-conditions/python) | 每步评估 `steps / errors / no_progress` → 显式 `stop_reason` | 与我们 `stopped_reason` 对齐 | — |
| [Typed Tool-Loop Detector](https://www.agentpatternscatalog.org/patterns/typed-tool-loop-detector/) | **在 dispatch 边界机械检测**，不靠 prompt 自觉 | 硬规则放 loop，不放「请勿重复」空话 | — |
| pydantic-deep StuckLoopDetection | identical / A-B-A-B / no-op；warn 默认优于立即 error | 默认 warn→换策，硬停兜底 | — |

**共识：**

1. **检测必须在工具 dispatch 边界**，prompt「不要重复」不可靠。  
2. **先警告注入观测，再硬停**；立即杀会误伤合法重复（分页、多次只读确认）。  
3. **不止 exact repeat**：至少还要 cycle（A↔B）与「同动作同错误」。  
4. **终止是多层信号**：目标达成 / 预算 / 病理 / 用户中断，按优先级检查。  
5. **Retry ≠ 再调一次同一工具**：strategy retry 要求换方法。

### 16.3 本项目现状（对照）

| 能力 | 状态 | 落点 | 缺口 |
|------|------|------|------|
| `max_steps` / 无 tool_calls 完成 / Ctrl+C | ✅ | `loop.py` | 病理早停偏晚 |
| `LoopGuard`：连续同 `(name, args)` ≥3 warn / ≥5 stop | ✅ | `loop_guard.py` + `stopped_reason=loop_detected`；`LOOP_*` 可配置 | 周期 A↔B 仍为 P2 |
| Prompt「出错请换方法」 | ✅ 弱 | system #10 | 建议性，无机械执行 |
| Todo / working_memory 焦点文件 | ✅ 部分 | Context Current State | — |
| 同轮多 tool 去重（复用结果） | ✅ | `LoopGuard` same-step cache；SSE `action_dedup` | — |
| Error-streak nudge | ✅ | 同 fingerprint 连续失败 ≥`LOOP_ERROR_NUDGE_AFTER`（默认 2） | — |
| TaskState MVP | ✅ | `task_state.py` → Current State + working_memory | — |
| 阶梯 Retry Policy | ✅ | `retry_policy.py`；stage3 → `retry_exhausted`；SSE `retry_stage` | — |
| 可验证完成（测试全绿 / todo 完 → 催 final） | ✅ | `stop_conditions.py`；再改 → `goal_met_forced` | — |
| 输出停滞 / 模糊相似 args | ❌ | — | P2 |

结论：**P0+P1 已落地。** 缺周期检测、soft-dedup、awaiting_user UX（P2）。

### 16.4 目标架构（三机制 + 终止层）

```text
用户任务
  │
  ├─ TaskState 初始化（Goal / StopCondition / RelevantFiles…）
  │
  └─ ReAct loop（每步）
        │
        ├─ ① Action Dedup（dispatch 前）
        │     exact / cycle / same-step cache
        │     → WARN 注入 / BLOCK 不执行 / STOP
        │
        ├─ ② 执行工具 → Observation
        │
        ├─ ③ Retry Policy（若失败或无进展）
        │     attempt 1: 允许同方向微调
        │     attempt 2: 强制换策略（注入「禁止 fingerprint X」）
        │     attempt 3: STOP → 请求用户（stopped_reason=retry_exhausted）
        │
        ├─ ④ 更新 TaskState（Completed / Failed / TestStatus / no_progress）
        │
        └─ ⑤ TerminationPolicy（优先级）
              External(Ctrl+C) > Pathology(loop/retry) > Goal(测试过 / todo 完) > Resource(max_steps)
```

**与现有模块落点：**

| 模块 | 职责 |
|------|------|
| `src/agent/loop.py` | 挂 Guard + Policy；扩展 `stopped_reason` |
| 新建 `src/agent/task_state.py`（建议） | TaskState 结构体 + 持久化到 `.agent/working_memory.json` 或 transcript |
| 新建或扩 `src/agent/loop_guard.py` | 从 loop.py 抽出；加 cycle / error-streak |
| `context_manager.py` | 每步把 TaskState 摘要注入 Current State（短、稳定） |
| Web SSE | 已有 `loop_warning`；可加 `retry_escalation` / `task_state` |

---

### 16.5 机制一：Action Deduplication

**检测键：**

```text
fingerprint = hash( tool_name + canonical_json(args) )
# canonical：sort keys、strip 空白、路径 normalize（相对 workdir）
```

**三层检测（由易到难）：**

| 层 | 规则 | 动作 |
|----|------|------|
| **L0 同轮去重** | 同一 LLM step 内相同 fingerprint 第二次 | **不执行**，复用第一次结果（或直接 BLOCK） |
| **L1 连续 exact**（已有） | 跨步连续相同 fingerprint | ≥3 WARN 后缀；≥5 `loop_detected` |
| **L2 滑动窗口 / 周期** | 窗口内同一 fingerprint ≥K；或 cycle 长度 2～4 重复 ≥R 次 | WARN → 再 STOP；覆盖 `read↔test` ping-pong |

**豁免（防误伤）：**

- `todo_write`：参数常「几乎相同」但状态字段变了——fingerprint 必须含完整 todos 内容。  
- 可选：`run_shell` 若 args 含时间戳/随机则自然不同；若完全相同仍应拦。  
- 配置项：`LOOP_WARN_AFTER` / `LOOP_STOP_AFTER`（默认 3/5），演示可调松。

**实现注意：**

- 拦截发生在 **PermissionGate 之后、handler 之前**（或之前也行，省审批弹窗）。  
- BLOCK 时仍要写一条 `role=tool` 结果，否则 messages 配对会坏（你们已踩过配对坑）。  
- 对「刚读过的文件」：可选 soft-dedup——若 fingerprint 在最近 W 步出现过且文件 mtime 未变，返回 `[dedup] 内容未变，上次摘要：…` 而非全文（省 token，属 15+16 交叉）。

---

### 16.6 机制二：Task State（显式任务状态）

**目的：** 给 harness 一个「世界进度条」，供注入 prompt、判停、判无进展；**不是**再造一个 Todo 工具（Todo 仍是模型可写的计划；Task State 是 harness 维护的只读/半自动状态）。

**建议字段：**

```json
{
  "goal": "修复 greeter 使 greeter_test 全部通过",
  "completed": ["定位失败用例", "最小修改 greeter.py"],
  "failed": [
    {"strategy": "edit_file:replace_print", "error": "AssertionError: ...", "count": 2}
  ],
  "current": "跑 pytest 验证",
  "relevant_files": ["greeter.py", "greeter_test.py"],
  "test_status": {
    "last_command": "python -m pytest greeter_test.py -q",
    "passed": false,
    "summary": "1 failed, 0 passed",
    "fingerprint": "pytest:greeter_test:1failed"
  },
  "stop_condition": "tests_all_pass",
  "no_progress_streak": 0,
  "retry_stage": 1
}
```

| 字段 | 谁更新 | 用途 |
|------|--------|------|
| `goal` | 任务开始时从 user message 截取 / 首轮推断 | 压缩后重注 |
| `completed` / `current` | 可由 todo 镜像，或启发式 | 催 final / 答辩可观测 |
| `failed[]` | 工具失败或测试失败时 append；同 strategy 累加 count | Retry Policy 输入 |
| `relevant_files` | `read/edit/write` 成功时维护（上限 N） | Context Current State |
| `test_status` | `run_shell` 输出像 pytest/unittest 时解析 | **Stop Condition** |
| `stop_condition` | 配置或任务类型默认 | `tests_all_pass` / `todo_all_done` / `user_declared` |
| `no_progress_streak` | 动作后「文件未变且测试未变」+1 | Pathology 停 |

**Stop Condition 示例：**

| 条件 | 触发 | 行为 |
|------|------|------|
| `tests_all_pass` | `test_status.passed == true` | 注入「测试已通过，请给出 FINAL，勿再改文件」；若模型仍调写工具 → WARN |
| `todo_all_done` | todos 均 completed 且无 pending 错误 | 同 15.6 P1「催 final」 |
| `same_failure × 3` | 同 strategy 失败 count≥3 | `retry_exhausted`，停止并请求用户 |
| `no_progress × N` | 连续 N 步无文件/测试变化 | 停止或强注入换策 |

**与 Context 的接缝：** Task State 摘要进 `Current State` 块（短）；完整 JSON 可进 `.agent/working_memory.json` / transcript `memory`，**不要**整份塞进每步 system（防炸窗口）。

---

### 16.7 机制三：Retry Policy（阶梯换策，禁止无限 retry）

**四类 Retry（答辩可用 Towards AI 分类，实现只做后两类为主）：**

| 类型 | 含义 | 本仓库策略 |
|------|------|------------|
| Transient | 网络 / 锁 / 偶发 | LLM API 层可有限次；工具层一般不自动重跑 |
| Format | JSON / 参数坏 | 回传错误，允许立刻改 args 再调 |
| Semantic | 工具成功但结果不对 | 算「策略失败」，进阶梯 |
| Strategy | 同一修法反复失败 | **本节核心** |

**阶梯（定案）：**

```text
同一 failure_key 第 1 次失败
  → 正常：把错误观测给模型，允许微调 args / 再读上下文

同一 failure_key 第 2 次失败
  → 换策：在 tool 结果或 system 附加强制指令：
    「禁止再次使用 strategy=… / fingerprint=…；
      请换：整段 rewrite / 另一文件 / 先写最小复现 / 询问用户」

同一 failure_key 第 3 次失败
  → 停止：stopped_reason=retry_exhausted
    最终回复明确：已尝试策略、最后错误、请用户指示
```

**`failure_key` 怎么定（实用启发式）：**

```text
failure_key ≈ tool_name + 目标文件 + 错误类名（或测试节点 id）
# 例：edit_file|greeter.py|AssertionError
# 例：run_shell|pytest|test_greet_name
```

不要用「整段错误信息」当 key（太碎会永远 stage=1）；也不要只用 tool_name（太粗会误伤无关调用）。

**与 Action Dedup 的分工：**

| | Dedup | Retry Policy |
|--|-------|--------------|
| 看什么 | 调用签名是否相同 | 失败是否同一策略 |
| 典型 | 连读同一文件 | 连改同一处仍红 |
| 成功路径 | 也会触发 | 只看失败 / 无进展 |

---

### 16.8 终止策略优先级（合并现有 `stopped_reason`）

建议扩展：

| `stopped_reason` | 含义 | 已有？ |
|------------------|------|--------|
| `completed` | 模型无 tool_calls 收尾 | ✅ |
| `max_steps` | 资源顶 | ✅ |
| `interrupted` | 用户 | ✅ |
| `loop_detected` | 病理：重复调用 | ✅ |
| `retry_exhausted` | 病理：同策略失败满级 | ❌ 新增 |
| `goal_met_forced` | 可选：测试已过仍空转后强制收束 | ❌ 可选 |
| `awaiting_user` | 软停：需要用户决策（Web 可做成可续写） | ❌ 可选 |

**检查顺序（每步末或每次工具后）：**

1. `interrupted`  
2. `loop_detected` / `retry_exhausted`  
3. Goal：测试全绿或 todo 完 → 催 final（先软后硬）  
4. `max_steps`

---

### 16.9 推荐策略（本仓库定案）

1. **在现有 `LoopGuard` 上长，不另起框架**；可抽到 `loop_guard.py`。  
2. **P0 补齐「看得见的转圈」**：同轮去重 + 错误 streak nudge + TaskState 最小字段注入。  
3. **P1 做阶梯 Retry + 测试 Stop Condition**（修 bug 演示收益最大）。  
4. **P2 再做 cycle 检测 / 模糊 args / awaiting_user UX**。  
5. **交付期**：Day4 录视频优先；本节默认交付后实施。循环检测 MVP 已可在视频里口头提一句。  
6. **度量**：构造「诱导转圈」用例（强制重复 read / 故意错误 edit 三次）写进冒烟或手工剧本。

### 16.10 改造清单

#### P0 — 夯实去重与状态可见（约 0.5～1 天）

- [x] **同轮 Action Dedup**：同一步相同 fingerprint → 复用结果或 BLOCK 文案  
- [x] **抽出 `LoopGuard` + 配置化** `warn_after` / `stop_after`  
- [x] **Error-streak nudge**（OpenHands 风格）：同 fingerprint 连续失败 ≥2 时注入「换策」文案（未达 stop 前）  
- [x] **TaskState MVP**：`goal` / `relevant_files` / `last_error` / `test_status`（粗解析 pytest 一行摘要）写入 working_memory，并注入 Current State  
- [x] **冒烟**：重复 read；同轮双 read；诱导失败 streak

#### P1 — 阶梯 Retry + 成功早停（约 1 天）

- [x] **Retry Policy**：`failure_key` + stage 1/2/3；stage3 → `retry_exhausted`  
- [x] **Stop Condition `tests_all_pass`**：与 15.6「Todo 催 final」一起做  
- [x] **failed strategies 列表**注入：禁止重复列出的 strategy  
- [x] SSE / CLI 展示 `retry_stage`、失败策略（可观测）

#### P2 — 周期与体验

- [ ] **Cycle 检测**（窗口内 A↔B 或长度 ≤4 的周期 ≥R）  
- [ ] Soft-dedup：mtime 未变则不回全文  
- [ ] `awaiting_user` + Web「继续 / 换目标」按钮  
- [ ] Fuzzy args（Jaccard）— 仅当 exact 误漏证据充足时再上

### 16.11 与各节关系（一览）

| 节 | 管什么 | 无效行动视角 |
|----|--------|--------------|
| 11 Context | 留什么、怎么折 | 不解决转圈；可承载 TaskState 摘要 |
| 13 Tool 编排 | 选什么工具 | 少选错；不挡重复选 |
| 14 Skill | 先验剧本 | 降低走进死胡同的概率 |
| 15 步数效率 | 正常路径少步 | 批工具 / 粗 todo |
| **16 本节** | **病理路径停转** | Dedup / State / Retry / Stop |

### 16.12 改造时注意（防踩坑）

1. **误伤合法重复**：分页、多次 `run_shell` 看不同输出——fingerprint 必须含全 args；必要时 per-tool 阈值。  
2. **BLOCK 也要写 tool 消息**：否则 pairing 再炸。  
3. **不要把 Retry 做成 harness 自动重跑同一 handler**：应让模型换调用；harness 只注入约束与计数。  
4. **Stop Condition 要可验证**：宁可用 pytest 摘要，也不要只信模型说「已修好」。  
5. **与 max_steps 并存**：病理停应早于预算耗尽；抬高 max_steps 不是解法。  
6. **改完后**：更新本节勾选、面试第 12 问、冒烟、进度日志；README 可加一行「循环 / 重试护栏」。

---

## 17. 安全 / 权限 / Verification：调研结论与改造方案

> 调研日期：2026-08-30。  
> **一句话：** Coding Agent 能真执行命令，安全不能只靠「提示词说会遵守」；要在工具边界做 **可执行的 Allow / Confirm / Deny**，并用 **证据（Evidence）** 决定能否宣称完成。  
> **与第 13 节关系：** 13 管「选什么工具」；本节管「允不允许真执行」+「完成是否可验证」。  
> **与第 16 节关系：** 16 的 Stop Condition（`tests_all_pass`）已是 Verification 雏形；本节把它升格为显式 Completion Criteria，并与权限管线并列。

### 17.1 问题定义：为什么 Coding Agent 必须单独做安全

Agent 不是「只会聊天」，而是会：

```text
rm -rf /   git reset --hard   pip install xxx
读 .env / SSH Key / API Key
```

现实威胁（答辩可对齐 OWASP）：

| 编号 | 名称 | 对本项目的含义 |
|------|------|----------------|
| [LLM06:2025 Excessive Agency](https://genai.owasp.org/llmrisk/llm062025-excessive-agency/) | 权限过大 | 每步都暴露全工具表 = 过度 agency |
| [ASI02 Tool Misuse](https://genai.owasp.org/)（Agentic Top 10） | 合法工具被误用 | 有权 `run_shell`，却跑了 `git reset --hard` |
| 间接 prompt injection | 文件/网页里藏指令 | 读到恶意 README 后去删文件、外传密钥 |
| 敏感文件泄露 | 凭据进 context | `.env` / `id_rsa` 一旦被 `read_file` 进 messages，后续难收回 |

**核心立场（对齐两篇论文）：**

- 安全规则必须由 **Harness 在工具执行前拦截**，不依赖模型「保证遵守」。  
- 自主程度是 **设计选择**（auto / ask / never），不是默认全开。

### 17.2 本项目现状（对照）

| 能力 | 状态 | 落点 |
|------|------|------|
| 工作目录路径沙箱 | ✅ | `PermissionGate.resolve_path` |
| Shell hard-deny（`rm -rf /`、fork bomb 等） | ✅ | `_HARD_DENY_PATTERNS` |
| 风险 shell + `--approval auto\|ask\|never` | ✅ | `_HIGH_SHELL_PATTERNS` + `ApprovalMode` |
| 变更类工具在 ASK 下确认 | ✅ | Medium/High 走 Confirm；Low 不打扰 |
| Tool 元数据 `risk_level` | ✅ | `FunctionTool` + `PermissionGate.bind_registry` |
| 敏感路径黑名单（`.env`、`*.pem`、`id_rsa`） | ✅ | `is_sensitive_path`；read/write/edit + shell `cat`/`type` |
| 按步 Least Privilege（动态收窄可见工具） | ✅ | `tool_visibility.py` + `TOOL_VISIBILITY=auto` |
| 网络 / `pip install` 分级策略 | ❌ | 一律走 shell 字符串匹配（P2） |
| Completion Criteria / Evidence Gate | ✅ | `completion_gate.py`；改文件后无测试绿则拒收束 |
| Web 下 ASK 确认 UX | 部分 | Web 无 Confirm 弹窗；**High 一律 Deny**（`deny_high`） |

结论：**P0+P1 已落地**（元数据 / 敏感路径 / 可见集 / Evidence Gate / Web High Deny）；缺口转到 P2（链式 IFC、网络策略等）。

### 17.3 目标管线（Permission → Risk → Decision → Tool）

```text
Agent 提出 tool_call
        ↓
┌───────────────────────┐
│  Permission Manager   │  路径沙箱 / 敏感路径 / 工具是否在本步可见集
└───────────┬───────────┘
            ↓
┌───────────────────────┐
│  Risk Assessment      │  读 Tool.risk_level + 参数启发式（命令模式、目标路径）
└───────────┬───────────┘
            ↓
     Allow / Confirm / Deny
            ↓
         Tool handler
            ↓
     Observation → messages
```

决策语义：

| 决策 | 含义 | 典型场景 |
|------|------|----------|
| **Allow** | 直接执行 | Low Risk 只读；Medium 在 `approval=auto` |
| **Confirm** | 问用户；拒绝则回写 tool 错误，不执行 | Medium/High 在 `approval=ask` |
| **Deny** | 不执行；写明 reason | hard-deny；敏感路径；`approval=never` 的高危 |

实现落点：仍集中在 `PermissionGate.authorize`（或拆 `risk.py` 给评估、Gate 只做决策），**禁止**在各 tool handler 里各自判权限。

### 17.4 三级风险（定案表）

| 级别 | 颜色 | 默认策略 | 本仓库工具 / 模式 |
|------|------|----------|-------------------|
| **Low** | 🟢 | 始终 Allow（仍受沙箱约束） | `list_dir` / `glob` / `read_file`（非敏感）/ `todo_write` / 只读检索类 |
| **Medium** | 🟡 | auto→Allow；ask→Confirm；never→Deny | `write_file` / `edit_file`；`run_shell` 跑测试（`pytest` / `python -m`）；`pip install`（可选再升 High） |
| **High** | 🔴 | ask→Confirm；never→Deny；部分 **永远 Deny** | 删除类；`git reset --hard` / `git push --force`；`rm -rf`；敏感文件读写；外网请求 / `curl\|sh` |

**参数级升级（重要）：** 工具名是 Medium，参数可把风险抬到 High。例：

- `read_file(path=".env")` → High / Deny  
- `run_shell("rm -rf demos")` → High  
- `run_shell("pytest greeter_test.py")` → Medium（或 Low，若配置「测试白名单」）

**敏感路径默认 Deny（建议清单）：**

```text
.env, .env.*, *.pem, *.key, id_rsa*, *.p12, credentials.json,
.ssh/**, **/secrets/**, 以及 workdir 外任何路径（已有沙箱）
```

业界对照：[Claude Code Permissions](https://code.claude.com/docs/en/permissions) 用 `allow` / `ask` / `deny` + 路径规则；敏感文件需 **permission deny +（理想情况）OS 沙箱** 双层——我们 Windows 上先做路径规则 + Gate，不强求 bubblewrap。

### 17.5 论文与业界调研（取长补短）

#### ① AgenTRIM — ⭐⭐⭐⭐⭐（最适合本项目）

- 论文：[AgenTRIM: Tool Risk Mitigation for Agentic AI](https://arxiv.org/abs/2601.12449)（Fujitsu Research，2026）  
- HTML：[arXiv HTML](https://arxiv.org/html/2601.12449)

**核心问题：** *unbalanced tool-driven agency*  
- **Excessive agency**：工具给太多 → 攻击面大（间接注入后能调到危险工具）  
- **Insufficient agency**：该有的安全/功能工具没有 → 任务失败或绕过策略  

**两阶段（我们怎么偷师，不照搬）：**

| 阶段 | 论文做法 | 本仓库简化落地 |
|------|----------|----------------|
| Offline | 从代码/轨迹抽取工具清单、验证、打 risk 标签 | 手写 Registry 元数据：`risk_level` / `is_readonly` / `permission`（不做自动 extractor） |
| Online | 每步自适应过滤可见工具 + 对 High 做 status-aware 校验 | P1：按 Todo 阶段 / 任务类型只暴露子集；High 走 Confirm；可选「当前 TaskState 未要求写文件则拒写」 |

**可抄的判决逻辑（答辩金句）：**

> Least Privilege：Agent **当前步只需要什么能力，就只给什么能力**；高风险调用必须相对 **任务状态** 再校验，而不是永远挂全工具表。

论文在 AgentDojo 上显示：收紧高风险暴露可大幅降攻击成功率，同时保留效用——叙事与我们「修 bug 演示」一致（读测改测，不必一直暴露危险 shell 模式）。

#### ② Towards Verifiably Safe Tool Use — ⭐⭐⭐⭐（ICSE 2026 NIER）

- 论文：[Towards Verifiably Safe Tool Use for LLM Agents](https://arxiv.org/abs/2601.08012)  
- 会议：[ICSE 2026 NIER](https://conf.researchr.org/details/icse-2026/icse-2026-nier/41/Towards-Verifiably-Safe-Tool-Use-for-LLM-Agents)

**核心转变：** 从「模型侧靠谱」→「边界上可执行、可验证的安全规范」。

方法骨架：

1. 用 **STPA** 找 hazard（例：密钥进 context、破坏性 git、外泄）  
2. 写成 **SPEC**（信息流 / 工具顺序约束）  
3. 四级执行：**Blocklist / Mustlist / Allowlist / Confirmation**  
4. 工具与数据带结构化标签：`capabilities` / `confidentiality` / `trust level`

**对本仓库的映射（务实版，不做 Alloy/MCP 扩展）：**

| 论文概念 | 我们落地 |
|----------|----------|
| Blocklist | hard-deny + 敏感路径 Deny |
| Allowlist | Low Risk 自动放行；测试命令白名单 |
| Confirmation | `approval=ask` + Medium/High |
| Mustlist | **Completion / Verification**：宣称完成前必须有测试证据（见 17.6） |
| Labels | Tool Registry 元数据；文件路径敏感标签 |

金句：**自主是 deliberate design choice；用户只在真正可能造成损失时被打扰**（避免确认疲劳）。

#### ③ 业界补充（实现时可对照）

| 来源 | 可借鉴点 | 我们不做 / 缓做 |
|------|----------|-----------------|
| Claude Code permissions + sandbox | allow/ask/deny 规则；敏感路径 deny；Bash 与文件工具规则对齐 | Linux/macOS 内核沙箱（Windows 用路径规则替代） |
| [agentpatterns: sensitive files](https://agentpatterns.ai/security/protecting-sensitive-files/) | deny 挡 Read/Edit；子进程仍可能读到 → 需沙箱或命令识别 | 先拦 `read_file`/`cat`/显式路径；子进程盲读标为已知局限 |
| WorkOS / OWASP ASI02 综述 | 调用策略四步：身份 → 授权 → **参数校验** → **链式上下文** | 身份层已是本地单用户；补参数与链式 |

### 17.6 Verification：完成必须有证据（强烈建议做）

普通 Agent：

```text
LLM：「代码修改完成。」 → 结束
```

好的 Coding Agent：

```text
LLM 改代码
   ↓
Harness 跑测试 / 看 exit code
   ↓
检查结果 +（可选）git diff / 文件存在
   ↓
Evidence 齐备
   ↓
才允许 Terminate / 接受「已完成」叙事
```

**Completion Criteria（建议默认，可配置）：**

| 条件 | 如何判定 | 未满足时 |
|------|----------|----------|
| ✓ 目标文件已写入/编辑成功 | transcript / `file_change` 事件 | 不允许说「已修复」 |
| ✓ 验证命令已执行 | 存在匹配的 `run_shell` 观测 | 注入「请先跑测试」 |
| ✓ 测试通过 / exit code 0 | 解析 pytest 摘要或 `returncode`（接 TaskState.`test_status`） | 继续 ReAct；禁止 final 空话 |
| ✓ 无明显硬错误 | stderr 无未处理 Traceback（启发式） | 回传错误，换策（接 16 Retry） |

**与 ICSE 论文的结合点（面试很好讲）：**

```text
Agent：「我完成了。」
        ↓
Harness（Mustlist / Evidence Gate）：
  pytest 绿？  git diff 非空或符合预期？  exit code？
        ↓
Evidence
        ↓
允许 Terminate  /  否则拒绝收束并注入下一动作
```

这把「安全可执行约束」从 **工具入口** 延伸到 **任务出口**：入口防危害，出口防撒谎。

**与第 16 节已有能力的关系：**

| 16 已有 | 17 升级 |
|---------|---------|
| `tests_all_pass` → final nudge | Evidence Gate：无证据则 **不接受** completed 叙事（可 `goal_met_forced` 仅在有证据时） |
| TaskState.`test_status` | 作为 Evidence 主来源 |
| `retry_exhausted` | 证据长期达不到 → 停并交给用户，而不是假完成 |

### 17.7 推荐架构（本仓库定案）

```text
                    ┌─ Tool Registry（risk_level / is_readonly / sensitive）
                    │
LLM ──tool_calls──►├─ Visibility Filter（本步可见工具 ⊂ 全量）  ← AgenTRIM 简化
                    │
                    ├─ PermissionGate
                    │     resolve_path + sensitive deny
                    │     RiskAssessment(tool, args) → L/M/H
                    │     Allow / Confirm / Deny
                    │
                    ├─ handlers …
                    │
                    └─ Evidence / CompletionGate（任务末）
                          pytest · exit · diff · file_change
                          → 允许 completed 或强制继续 / 停
```

**配置面（保持简单）：**

| 配置 | 作用 |
|------|------|
| `--approval auto\|ask\|never` | 全局自主度（已有） |
| `RISK_POLICY=strict\|normal` | strict：Medium 也 Confirm；pip/网络升 High |
| `SENSITIVE_GLOBS=...` | 敏感路径（默认内置列表） |
| `COMPLETION_MODE=evidence\|trust_model` | 默认 `evidence`（演示/答辩用） |

Web：P1 补「Confirm」弹窗或会话内审批；在此之前 Web 默认 `never` 对 High + auto 对 Low/Medium，或继续 auto 但 **High 一律 Deny**（比静默 AUTO 更安全）。

### 17.8 改造清单

#### P0 — 元数据 + 敏感路径 + 风险表（约 0.5～1 天，答辩收益最高）

- [x] `FunctionTool` / Registry 增加 `risk_level: low|medium|high`、`is_readonly: bool`  
- [x] `PermissionGate.authorize` **优先读元数据**；参数启发式可升级风险  
- [x] 敏感路径 Deny：`.env*` / `*.pem` / `id_rsa*` / `.ssh/**` 等（read/write/edit + shell 里识别的 `cat`/`type` 目标）  
- [x] 扩展 High 模式：`git reset --hard`、`git push --force`、递归删除、管道执行脚本（已有部分）  
- [x] 冒烟：试图读 `.env`、跑 `git reset --hard`、正常 `pytest` 应分别 Deny / Deny或Confirm / Allow  
- [x] 更新面试第 5 问与 README「安全」小节

#### P1 — Least Privilege 可见集 + Completion Evidence（约 1 天）

- [x] 按阶段收窄 tools：例如 Todo 标为「只读定位」时不暴露 `write_file`；「验证」阶段优先测试相关 shell  
- [x] CompletionGate：模型无 tool_calls 收尾时，若 `COMPLETION_MODE=evidence` 且任务像修 bug，则检查 `test_status`；不齐则注入「先跑测试 / 证据不足」而非直接 `completed`  
- [x] CLI Confirm 已有；Web：High 默认 Deny（`deny_high=True`），Medium 仍 auto  
- [x] transcript / SSE 记录 `auth_decision`（allow/confirm/deny + reason）便于答辩指日志

#### P2 — 加深（交付后 / 有余力）

- [ ] 链式策略：同会话「刚读敏感 → 禁止外传类 shell」（简化 IFC）  
- [ ] `pip install` / 网络独立策略开关  
- [ ] 测试命令白名单（Medium→Low）减少 ask 疲劳  
- [ ] Windows 下可选 Job Object / 更严工作目录（不承诺 OS 沙箱）  
- [ ] 形式化 / Alloy：仅作面试「了解」，不进仓库

### 17.9 时间建议（结合交付）

| 窗口 | 建议 |
|------|------|
| Day4 录视频前 | **至少口头能讲** 现有沙箱 + approval；若只改代码做一件：敏感路径 Deny（改动小、演示冲击大） |
| 交付后 / 加分项 | P0 全做 + CompletionGate 接上 16 的 `test_status` |
| 不要做 | 完整 AgenTRIM offline extractor、MCP 标签协议、Alloy 验证器 |

### 17.10 与各节关系（一览）

| 节 | 管什么 | 安全视角 |
|----|--------|----------|
| 13 Tool 编排 | 选什么工具 | 可见集缩小 = Least Privilege 的一半 |
| 14 Skill | 任务剧本 | Skill **不得**绕过 Gate |
| 15 效率 | 少步 | 白名单测试命令可少 Confirm |
| 16 无效行动 | 停转 / Stop | Evidence 与 `tests_all_pass` 同源 |
| **17 本节** | **能否执行 + 能否宣称完成** | Allow/Confirm/Deny + Evidence Gate |

### 17.11 改造时注意（防踩坑）

1. **确认疲劳**：事事 Confirm → 用户全按 Y。High 才 Confirm；Low 绝不打扰。  
2. **Deny 也要写 tool 消息**：与 16 一样，否则 `tool_call_id` pairing 断裂。  
3. **子进程绕过**：`python -c "open('.env')"` 难靠字符串穷举——文档写明局限；演示用直接 `read_file .env` 即可。  
4. **Skill / Todo 不提权**：任何「自动批准」只能降用户打扰，不能绕过 hard-deny。  
5. **Evidence 勿过严**：纯问答任务不要强制 pytest；用任务类型或 `stop_condition` 区分。  
6. **改完后**：勾选本节、冒烟、进度日志；面试第 5 / 12 问与第 13 风险「风险元数据」对齐。

### 17.12 精读顺序（改代码前 1～2 小时）

1. 本仓库 `src/agent/permissions.py` + `loop.py` 里 `authorize` 调用点  
2. AgenTRIM 摘要：§1 问题 + §3.2 Online orchestrator（[HTML](https://arxiv.org/html/2601.12449)）  
3. ICSE 文 §4.2 四级 enforcement + §4.3 labels（[arXiv](https://arxiv.org/abs/2601.08012)）  
4. Claude Code：[Permissions](https://code.claude.com/docs/en/permissions) 中 allow/ask/deny 与敏感文件一节（对照设计，不抄配置格式）
