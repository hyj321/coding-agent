# 编程智能体（Code Agent）计划书

> 本文档随开发进度持续更新。每次改代码后，请在文末「进度日志」（第 12 节）追加一条记录，并勾选对应任务。长短期记忆后续改造见 **第 11 节**。

**截止时间：** 2026-09-02 24:00（北京时间）  
**当前阶段：** 开发主体已完成，进入 **Day4 交付**（录视频 + README.txt + 推送/打包提交）  
**目标形态：** 自研 Agent Harness + DeepSeek tool calling + 本地工具箱 + Todo 特色 + CLI/Web 双入口 + 可观测 timeline  

**学术诚信提醒：** 开源项目仅作架构学习，禁止整仓复制或「套壳」提交；核心循环、工具执行、上下文与安全逻辑必须自己实现并能答辩。

---

## 0. 已完成总览（截至 2026-08-29）

> 本节汇总「现在已经做成什么」，方便对照题目与答辩。未完成项见第 6 节 Day4。

### 0.1 对照题目硬性要求

| 题目要求 | 状态 | 落点 |
|----------|------|------|
| 自主读写文件、执行命令 | ✅ | `read_file` / `write_file` / `edit_file` / `run_shell` 等，本地执行 |
| 对话历史与上下文管理 | ✅ | `messages` 拼接；`trim_messages`；工具输出截断；transcript |
| 工具定义与本地执行 | ✅ | `ToolRegistry`（schema + handler）；无 Code Interpreter |
| 模型输出解析 | ✅ | 解析 `tool_calls` / 参数 JSON / `tool_call_id` |
| 循环终止条件 | ✅ | 无 tool_calls 结束 / `max_steps` / Ctrl+C |
| 错误处理 | ✅ | 工具错误回传模型；沙箱；hard-deny；approval |
| 不用 agent 框架 | ✅ | 仅用 OpenAI 兼容客户端调 DeepSeek |
| 凭据不入库 | ✅ | `.env` + `.gitignore`；`.env.example` 无密钥 |
| 公开 Git 仓库 | ✅ | https://github.com/hyj321/coding-agent （后续改动需再 push） |

### 0.2 已实现能力清单

**核心 Harness**

- Agent 主循环（`src/agent/loop.py`）+ 可选 `on_event` 结构化事件  
- DeepSeek / OpenAI 兼容 LLM 客户端（`src/llm/client.py`）  
- 配置：`DEEPSEEK_API_KEY` / `BASE_URL` / `MODEL` / `MAX_STEPS` / `MAX_MESSAGES` / `APPROVAL`  

**工具（7 个）**

- `read_file` / `write_file` / `edit_file` / `list_dir` / `glob` / `run_shell` / `todo_write`  

**安全与稳定性**

- 工作目录路径沙箱  
- `--approval auto|ask|never` + 危险命令 hard-deny  
- 上下文裁剪、工具输出截断、transcript JSON 落盘  
- **ACON 简化 Context Manager**：观测压缩 + 分层折叠（`CONTEXT_TOKEN_BUDGET`）  

**特色**

- **Plan-then-Act**：`todo_write` + system prompt「先计划再执行」  

**交互入口**

- **CLI**：`python -m src.main -w demos "..."`  
- **Web UI**：`python -m src.web` → http://127.0.0.1:7860  
  - 仿现代聊天产品布局（非套壳第三方 agent，仍调同一套 loop）  
  - SSE 推送步骤；步骤卡片（完成可折叠、进行中居中高亮）  
  - 左侧栏固定 + Recent 独立滚动；**session 级历史**（一大轮对话一条记录，可多轮续写）  
  - Open folder → 更新 workdir；右侧 Files 树 + 新窗口看文件；底部 Changed files + 新旧 diff  
  - Todo / Plan 面板；Markdown 富文本；Reset demos；单任务锁（并发第二次 409）  

**演示与质量**

- 定稿演示：`demos/greeter.py` + `greeter_test.py`（见 `demos/DEMO.md`）  
- 另有 `buggy_calc.py` 等；`POST /api/demos/reset` 可恢复有意 bug  
- 离线冒烟：`python -m scripts.smoke_v1`  
- 仓库 README 含架构说明与运行方式  

### 0.3 尚未完成（交付相关）

- [ ] 本地录制 ≤2 分钟演示视频  
- [ ] 撰写提交用 `README.txt`（≤1000 汉字）  
- [ ] 将 Day2 之后代码全部 `git push` 到 GitHub（保持提交历史）  
- [ ] 打「姓名.zip」（仅 README.txt + 视频）并上传表格  

### 0.4 怎么跑（速查）

```powershell
conda activate codeagent   # 或 .venv
cd G:\codeagent
pip install -r requirements.txt
# 配置 .env 中的 DEEPSEEK_API_KEY

# CLI
python -m src.main -w demos --approval auto "阅读 greeter_test.py，修复 greeter.py…先用 todo_write…"

# Web
python -m src.web
# 浏览器打开 http://127.0.0.1:7860 ，建议 Ctrl+F5
```

---

## 1. 题目要求摘要

个人独立实现一个编程智能体：通过与大语言模型交互，自主读写文件、执行命令，完成编程任务。

### 硬性约束

- 禁止在现成 agent 产品上封装界面
- 禁止使用 agent 框架 / SDK（LangChain、LlamaIndex、OpenAI Agents SDK、Claude Agent SDK、AutoGen、CrewAI 等）
- 允许：模型厂商 API 客户端、OpenAI 兼容网关、原生 tool calling
- 禁止依赖服务端托管的代码执行或文件工具（Code Interpreter、Files API 等）
- 必须自研：对话历史与上下文管理、工具定义与本地执行、模型输出解析、循环终止条件、错误处理
- API Key 等凭据只能走环境变量或未入库配置，不得出现在仓库 / README / 视频中

### 提交物

1. **公开 Git 仓库**（题目发布后新建，保留完整提交历史）
2. **README.txt**（≤1000 汉字）：仓库地址、如何运行、特色功能等
3. **视频**（≤2 分钟，mp4，≤200 MB）：演示真实编程任务 + 简要讲解实现
4. 最终打成「姓名.zip」（仅含 README.txt + 视频）提交

### 面试关注点

能否解释 agent 为何这样运转，并为设计决策辩护。

---

## 2. 开源调研：别人怎么做、我们学什么

> 调研日期：2026-08-29。结论：优秀作品的共同点不是「功能最多」，而是 **loop 极简 + harness 分层清晰 + 安全/上下文/可观测三者至少做实**。

### 2.1 参考项目一览

| 项目 | 链接 | 体量/定位 | 优点 | 对我们的启示 | 4 天内不要照搬 |
|------|------|-----------|------|--------------|----------------|
| **ai-coding-agent**（你提到的） | [RivaanRanawat/ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) | 功能较全的 Claude Code 风格 | 模块划分清楚（agent/client/tools/context/safety/hooks/ui）；有审批策略、危险命令检测、会话存档、循环检测 | 目录可参考；「审批 + 安全 + transcript」值得学 | MCP、子 agent、复杂 Hooks、完整 TUI 命令体系 |
| **CoreCoder** | [he-yufeng/CoreCoder](https://github.com/he-yufeng/CoreCoder) | ~1000 行「nanoGPT of coding agents」 | 把本质说透：while 循环 + 若干工具；工具可扩展；可读性极强 | **主学习对象**：先读懂再自写；八工具里优先学 bash/read/write/edit/glob/grep/todo | 子 agent 工具、完整 CLI 包装 |
| **mini-coding-agent** | [rasbt/mini-coding-agent](https://github.com/rasbt/mini-coding-agent) | 教学向 harness | 明确六块：workspace 快照、稳定 prompt、结构化工具+权限、transcript/memory、有界委托 | 面试可用「六组件」叙事；`--approval` / `--max-steps` 设计很干净 | 子 agent 委托、复杂 memory |
| **learn-claude-code / mini-claude-code** | [shareAI-lab](https://github.com/shareAI-lab/mini-claude-code) / [Learn Claude Code](https://learn-cc-agent.vercel.app/en/) | 渐进教程 v0→v4 | v0 只有 bash → v1 四工具 → v2 Todo → v3 子 agent → v4 Skills | **开发顺序就按这个爬坡**，不要第一天做满 | Skills / 子 agent（时间不够就停在 v1+Todo） |
| **agent-zero-to-hero** | [KeWang0622/agent-101](https://github.com/KeWang0622/agent-101) | 课程式从零搭建 | 强调「整个 loop 就几行」；无框架；多 provider | 证明作业完全可自研；章节式理解利于答辩 | 不必做完 20 章 |
| **Harness 六层文章** | [FutureAGI: 6 Core Layers](https://futureagi.com/blog/how-to-build-a-coding-agent-harness/) | 架构说明 | Provider / Loop / Tool Registry / Context / Sandbox / Permissions | 用「六层 harness」写进 README 与面试稿 | Linux bubblewrap 级沙箱（Windows 上改用路径沙箱即可） |
| **Edd Mann 实践文** | [Around the Loop](https://eddmann.com/posts/around-the-loop-building-a-coding-agent-harness-in-python/) | 个人完整 harness 复盘 | 权限用 hook 切入；Skills；会话持久化；子 agent 取最简端 | 「权限检查插在工具执行前」比堆功能更专业 | 完整插件协议、深嵌套子 agent |

### 2.2 业界共识（几乎所有项目都承认）

核心只有这一段（OpenAI / Anthropic 协议细节不同，语义相同）：

```text
while True:
    response = model(messages, tools)
    if 没有 tool_calls:
        return 最终文本
    本地执行 tools → 把结果 append 进 messages
    # 必须有 max_steps，防止死循环
```

**难点不在 loop，而在 harness：** 工具边界、上下文膨胀、安全审批、失败恢复、过程可观测。

### 2.3 可直接借鉴的「好设计」（按优先级）

| 优先级 | 设计点 | 学自何处 | 我们怎么落地 |
|--------|--------|----------|--------------|
| P0 | 工具注册表：schema + handler 成对 | CoreCoder / FutureAGI | `tools/base.py` 注册，loop 只 `dispatch(name, args)` |
| P0 | 多终止条件：正常结束 / max_steps / 用户中断 | 几乎所有人 | Day1 就写进 loop |
| P0 | 工具错误变成字符串回传模型 | 所有能自我修复的 agent | `try/except` → `Error: ...` |
| P0 | 过程日志：每步打印工具名与摘要 | ai-coding-agent / 视频需求 | 彩色或前缀日志，方便录屏 |
| P1 | `edit` 优于整文件 `write` | CoreCoder / Claude Code 系 | Day2 加 `edit_file` |
| P1 | 工具输出截断 + 历史裁剪 | rasbt / FutureAGI compaction | 先截断 stdout；再丢弃最旧中间轮 |
| P1 | 工作区沙箱 + 危险命令确认 | ai-coding-agent / FutureAGI Permissions | Windows 上做路径限制 + 命令黑名单/ask |
| P1 | Transcript 落盘（JSON/JSONL） | MindStudio harness / rasbt | 每次 run 一份，便于复盘与视频 |
| P2 | Todo / 显式计划（防跑偏） | learn-claude-code v2、CoreCoder `todo_write` | **推荐作为特色 B** |
| P2 | Workspace 快照注入 system prompt | rasbt | 启动时列顶层目录 + 说明 cwd |
| P2 | 循环检测（重复同一工具调用） | ai-coding-agent | 小而美，面试好讲 |
| P3 | grep/glob | CoreCoder | Day2–3 有空再加 |
| 不做 | MCP / 多子 agent / Skills 生态 / 复杂 TUI | 各大型项目 | 时间成本高，答辩难讲透 |

### 2.4 别人「不全」或「过大」的地方 → 我们如何做得更好

针对 [ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) 这类「看起来全但未必适合作业」的项目：

| 常见问题 | 更好的做法（本项目） |
|----------|----------------------|
| 功能面铺太开（MCP、子 agent、hooks…），核心反而难讲清 | **深度 > 广度**：六层 harness 每层都能画图讲解 |
| 缺「真实可复现演示任务」 | 自带 `demos/` 小项目 + 固定任务脚本，视频一次过 |
| 安全停留在开关，缺少默认安全路径 | **默认沙箱**（只能碰工作目录），危险操作为 opt-in |
| 上下文策略含糊，长任务易崩 | 明确两级：① 工具输出截断 ② 历史 K 轮保留；写进 README |
| 换模型不友好 | 从第一天就用 **OpenAI 兼容客户端 + DeepSeek**，只换 env |
| 文档像功能清单，不像设计说明 | README/面试稿强调 **为什么这样停、为什么错误要回传、为什么要裁剪** |

**差异化口号（写进最终 README 可用）：**

> 一个能讲清楚的小型 coding agent harness：DeepSeek tool calling + 本地工具执行 + 可观测循环 + 默认路径沙箱；特色做深「Todo 规划」或「测试驱动修复」，而不是堆 MCP。

### 2.5 建议精读顺序（编码前 2–3 小时）

1. [Learn Claude Code 核心 loop](https://learn-cc-agent.vercel.app/en/)（建立直觉）  
2. [FutureAGI 六层 harness](https://futureagi.com/blog/how-to-build-a-coding-agent-harness/)（建立分层词汇）  
3. 浏览 [CoreCoder](https://github.com/he-yufeng/CoreCoder) 的 `agent` 主循环与 `tools/`（看「最小能跑」长什么样）  
4. 扫一眼 [rasbt/mini-coding-agent](https://github.com/rasbt/mini-coding-agent) 的六组件命名（方便写文档）  
5. [ai-coding-agent](https://github.com/RivaanRanawat/ai-coding-agent) 只看 `safety` / `context` / 工具列表，**不要抄全仓结构**

---

## 3. 系统组件（对齐「六层 Harness」）

业界常用六层；我们在 Windows + 4 天约束下做**精简落地版**：

```
用户输入（CLI）
   ↓
┌─────────────────────────────────────────┐
│  Delivery：CLI / 日志 / 可选确认         │
├─────────────────────────────────────────┤
│  Permissions：路径沙箱 + 危险命令策略     │
├─────────────────────────────────────────┤
│  Context：system 组装 + 历史裁剪 + 截断   │
├─────────────────────────────────────────┤
│  Tools：Registry（schema ↔ handler）     │
├─────────────────────────────────────────┤
│  Agent Loop：call → tool_calls → append  │
├─────────────────────────────────────────┤
│  Provider：DeepSeek（OpenAI 兼容）       │
└─────────────────────────────────────────┘
```

| 组件 | 职责 | 面试一句话 |
|------|------|------------|
| Provider | chat + tools API | 只当客户端，不含 agent 逻辑 |
| Agent Loop | 调模型 → 执行工具 → 写回历史 | 没有框架，就是 while + 终止条件 |
| Tool Registry | schema 与本地函数成对 | 模型只出意图，副作用在本地 |
| Context | 防爆上下文、组装 system | 截断输出 + 保留最近 K 轮 |
| Permissions | 限制能碰什么 | 默认工作目录沙箱，危险命令可确认 |
| CLI | 可观测与交互 | 每一步工具调用都看得见 |

---

## 4. 功能清单

### 4.1 必备功能（MVP = learn-claude-code 的 v1 水平）

- [x] Agent 主循环（调模型 → 执行 tool_calls → 写回历史 → 再调）
- [x] 终止条件：无工具调用的最终回复 / 达到最大步数 / 用户中断
- [x] 工具：`read_file`
- [x] 工具：`write_file`
- [x] 工具：`list_dir`（或启动时 workspace 快照二选一，建议都有）
- [x] 工具：`run_shell`
- [x] 对话历史按 system / user / assistant / tool 正确拼接（含 `tool_call_id`）
- [x] 工具失败时把错误返回给模型（不直接崩溃）
- [x] 配置：`DEEPSEEK_API_KEY`（或通用 `API_KEY`）/ `BASE_URL` / `MODEL` 走环境变量
- [x] 可指定工作目录
- [x] 终端打印工具名、参数、结果摘要（过程可见）

### 4.2 强烈建议（稳定性与作品感 = harness 合格线）

- [x] `edit_file` / search_replace（避免整文件重写）
- [x] 路径沙箱（限制在 project root 内）—(V1 已做)
- [x] 工具输出截断（尤其 shell）
- [x] 上下文裁剪（保留 system + 最近 K 轮）
- [x] 危险操作二次确认（`--approval ask|auto|never`）
- [x] 运行 transcript 落盘（JSON）
- [x] 启动时把 cwd 顶层结构写入 system prompt（轻量 workspace 快照）

### 4.3 特色功能（只选 1 个做深）

调研后重新排序的候选：

- [x] **A. Todo / Plan-then-Act（最推荐）** — 已实现 `todo_write` + system prompt 规划约束  
- [ ] **B. 测试驱动修复** — 改完自动跑测试，失败继续修；视频效果极强  
- [ ] **C. 循环检测** — 学自 ai-coding-agent：检测重复工具调用并打断；实现小、差异化清晰  
- [ ] D. 多 tool_calls 一次执行 + 清晰分步日志（工作量小于上面，可作辅亮点）

**已选定特色：** A. Todo / Plan-then-Act（`todo_write`）

### 4.4 扩展功能（有时间再做；P3）

- [x] `grep` / `glob` 搜索工具 —（Day2 已加 `glob`；`grep` 仍可选）
- [x] 简单摘要式 compaction（超长任务）— ACON 简化版 Context Manager：观测压缩 + 分层折叠 + history summary
- [ ] 流式输出
- [x] 会话 save/resume — Web session_*.json 多轮续写
- [x] Web UI（非必须）— `python -m src.web`：SSE 步骤卡片、Todo/Files 右栏、session 历史、Open folder、Changed files diff、Reset demos、Markdown、步骤折叠 / 进行中居中
- [x] **长短期记忆增强（交付后 / 有余力）** — 详见 **第 11 节**（**P0–P2 已做**；云端 embedding / 厂商 KV compaction 仍依赖网关能力）

---

## 5. 技术选型

| 项 | 选择 | 备注 |
|----|------|------|
| 语言 | **Python 3.11+**（已定） | 与多数教学仓一致，工具好写 |
| API | **DeepSeek**（OpenAI 兼容 tool calling，已定） | `base_url=https://api.deepseek.com`；默认 `deepseek-v4-flash` |
| SDK | 官方 `openai` 客户端即可 | 允许使用；不算 agent 框架 |
| 交互 | **CLI + Web UI** | Web 可选；核心仍是同一套 harness |
| Agent 框架 | **不使用** | 符合题目要求 |

### 5.1 当前目录结构

```
codeagent/
  README.md / 计划书.md / requirements.txt / .env.example
  src/
    main.py                 # CLI
    config.py
    agent/                  # loop / context / permissions / transcript
    llm/client.py
    tools/                  # base / filesystem / shell / todo
    web/                    # FastAPI + static UI
  demos/                    # greeter、buggy_calc、DEMO.md …
  scripts/smoke_v1.py
  transcripts/              # gitignore
```

### 5.2 扩展性约定

- 新工具 = 实现函数 + 注册 schema，主循环不改  
- 换模型 = 只改 `BASE_URL` / `MODEL`  
- 新模式（Todo / 测后修复）= 新工具或 loop 外薄包装，不推翻重写  
- Permissions 独立模块：以后加规则不必改工具内部逻辑  

---

## 6. 四天周期计划（按调研后的爬坡路径）

对齐 learn-claude-code：**v0/v1 → 稳定 harness → 一个特色 → 交付**。

### Day 0–1（8/29–8/30）：MVP 跑通（= v1）

**目标：** 一句话任务能让 agent 自己写文件并执行成功。

- [x] 新建公开 Git 仓库并完成首次提交（https://github.com/hyj321/coding-agent ）  
- [x] `config` + DeepSeek LLM client（非流式 chat + tools）  
- [x] Tool registry + `read` / `write` / `list_dir` / `run_shell`  
- [x] Agent loop + max_steps + 错误回传 + 日志  
- [x] CLI 入口可用（`python -m src.main`）  
- [x] 路径沙箱  
- [x] 离线冒烟：`python -m scripts.smoke_v1` 通过  
- [x] **端到端验收**：`python -m src.main -w demos "...创建 hello.py 并运行"` → 3 步完成，`stopped_reason=completed`  

**验收：** 无人工干预完成「写一个脚本并运行成功」。 ✅ 2026-08-29 已通过

### Day 2（8/31）：Harness 合格线

- [x] `edit_file`  
- [x] 工具输出截断 + 上下文裁剪（`trim_messages` / `MAX_MESSAGES`）  
- [x] workspace 快照进 system prompt  
- [x] approval / 危险命令策略（`auto|ask|never` + hard-deny）  
- [x] transcript 保存（`transcripts/*.json`）  
- [x] `glob` 搜索工具  

**验收：** 能对已有小项目修 bug / 加小功能，中间报错能自我恢复。  
✅ 2026-08-29：修复 `demos/buggy_calc.py` — `read_file` → `edit_file` → `run_shell` 验证通过（4 步）。
### Day 3（9/1）：一个特色做深 + 演示定稿

- [x] 实现选定特色：**Todo / Plan-then-Act（`todo_write`）**  
- [x] 定稿 `demos/` 演示任务（见 `demos/DEMO.md`：`greeter.py` + `greeter_test.py`）  
- [ ] 试录视频：日志清晰、任务有头有尾（**待你本地录制**）  
- [x] 补一段「架构说明」到仓库 README  

**验收：** 同一演示任务稳定复现；你能不看稿讲清六层里你实现了哪几层。  
✅ 2026-08-29 端到端：先 `todo_write` → 读文件 → `edit_file` → 跑测试 `ok` → todo 全完成（5 步）。演示源码已恢复为有意 bug，便于复录。

### Day 4（9/2）：交付

- [ ] README.txt（≤1000 字）  
- [x] 仓库 README（安装、环境变量、示例、设计要点、Web 说明）— 已有，提交前可再润色  
- [ ] 视频（≤2 分钟）  
- [x] `.gitignore` 含 `.env`（勿提交密钥）  
- [ ] 将最新代码 push 到 GitHub → 打 zip 提交  
- [ ] **当天不再加新大功能**  

---

## 7. 演示任务（已定稿）

**设计原则：** 任务必须用工具；包含读代码 → 改代码 → 跑命令验证；并体现 Todo 规划。

**最终演示任务：** 修复 `demos/greeter.py`，使 `greeter_test.py` 全部通过；过程中使用 `todo_write`。

**复现命令：**

```powershell
conda activate codeagent
cd G:\codeagent
python -m src.main -w demos --approval auto --max-steps 20 "阅读 greeter_test.py，修复 greeter.py 使测试全部通过。请先用 todo_write 列出计划，再逐步执行并更新 todo 状态，最后运行 python greeter_test.py 验证。"
```

详见 `demos/DEMO.md`。

---

## 8. 面试预案（设计决策）

开发过程中把「为什么」写清楚，面试直接用：

1. **主循环如何停止？**  
   _答：无 tool_calls 的最终回复 / 达到 max_steps / 用户中断。（可补充：循环检测打断）_

2. **工具结果为什么必须进 messages？**  
   _答：模型看不见本地副作用，只能通过返回内容感知；`tool_call_id` 用于对齐请求与结果。_

3. **上下文爆了怎么办？**  
   _答：两级。① 工具观测压缩（pytest 等只留失败卡片，`compress_tool_result`）；② ACON 思路的 Context Manager：超 `CONTEXT_TOKEN_BUDGET`（默认 8000）时折叠中间轮为 Historical Context，保留目标 / Current State（焦点文件、错误、todo）/ 最近工具轮。完整对话另存 `transcripts/`（含 `memory` 快照）。_

4. **为什么不用 LangChain？**  
   _答：题目禁止；自研可完全控制循环、错误回传与终止；也与 CoreCoder 等教学路径一致。_

5. **如何防止危险操作？**  
   _答：路径必须落在 workdir；`PermissionGate` 对 `rm -rf /` 等 hard-deny；`--approval ask|never` 可拦截风险 shell / 变更操作。_

6. **特色 Todo 为什么有用？**  
   _答：模型容易中途跑偏；`todo_write` 把计划变成可观察状态，同一 loop 内边做边勾选，答辩时能指着日志讲「先计划后执行」。_

7. **你和「功能很多的开源 agent」差在哪？**  
   _答：故意不做 MCP/子 agent，把 Todo、安全与可观测做深，保证每一行都能答辩。_

8. **长短期记忆怎么做的？还缺什么？**  
   _答：短期 = Context Manager（观测压缩 → MicroCompact stub → 超预算折叠 → 重注根状态；稳定前缀/可变后缀）；ACON-lite 用失败对收紧 `.agent/compress_guideline.json`。会话 = 续写瘦身 + working_memory.json。长期 = MEMORY.md + memory_search（关键词）+ rag_search（本地 TF–IDF）。厂商 KV-cache / 服务端 compaction：已挂 cache hint，真实命中看网关；本地 fold 作 fallback。未做：云端 embedding、ACON 离线蒸馏小压缩器。_

---

## 9. 风险与对策

| 风险 | 对策 | 状态 |
|------|------|------|
| 被开源项目带偏，功能贪多 | 锁定 P0/P1；特色只选 1 个；MCP/子 agent 不做 | ✅ 已遵守 |
| 模型不听话乱调工具 | 清晰 system prompt + 工具 schema；Todo 约束步骤 | ✅ 已落地 |
| Shell 输出过长 | 截断 + 提示用更精确命令 | ✅ 已截断 |
| 时间不够 | 死守 Day1 MVP；Day3 只做特色 | ✅ 主体完成 |
| Key 泄漏 | 环境变量 + 提交前检查 | ⚠️ 每次 push 前再查 |
| 视频超时 | 脚本化演示；中间过程加速 | 待录制 |
| 整段抄袭开源 | 只学架构，自写实现；能讲清每一模块 | 待遵守 |

---

## 10. 待确认事项

- [x] 确认语言：Python  
- [x] 确认模型：DeepSeek（flash 开发 / pro 演示）  
- [x] 确认特色功能：A Todo / B 测后修复 / C 循环检测 — **已选 A**  
- [x] 确认演示任务（见第 7 节 / `demos/DEMO.md`）  
- [x] 确认公开仓库平台（GitHub / Gitee）与仓库名 — `https://github.com/hyj321/coding-agent`  
- [x] 配置本地 `.env` 并完成 V1 端到端验收  

---

## 11. 长短期记忆：调研结论与后续改造清单

> 调研日期：2026-08-29。用途：交付后或有余力时按 P0→P1→P2 改；**Day4 交付前勿再开大坑**。  
> 心智模型：**记忆 = context engineering**（决定哪些 token 进窗口），不是无限上下文。

### 11.1 业界共识分层

| 层级 | 类比 | 放什么 | 本项目现状 |
|------|------|--------|------------|
| **Working / Core（短）** | RAM | 当前目标、焦点文件、最近工具、todo、最近错误 | ✅ `ContextManager`（Current State + Recent + 观测压缩） |
| **Episodic / Recall（中）** | 日记 | 完整对话可回放/续写 | ✅ `transcripts/session_*.json` + Web 同会话 `prior_messages` |
| **Semantic / Archival（长）** | 硬盘 | 跨会话稳定事实、偏好、项目约定 | ✅ 项目级 `MEMORY.md`（开局注入 / 结束追加） |
| **Compaction** | GC | 窗口满了摘要/裁剪 | ✅ 超 `CONTEXT_TOKEN_BUDGET` 折叠为 Historical Context |

**重要区分：** RAG 是把外存捞回窗口的一种手段，**不等于记忆本身**；记忆还要写、更新、压缩。Coding agent 里「主动 `read_file` / `glob`」也是检索。

### 11.2 值得学的设计（精读）

| 思路 | 核心做法 | 链接 | 对我们的启示 |
|------|----------|------|--------------|
| **MemGPT / Letta** | Core blocks 常驻；Archival 向量检索；agent 用工具自改记忆 | [Agent Memory](https://www.letta.com/blog/agent-memory/) | 长期用「可编辑块」比先上向量库更贴作业 |
| **Claude Code Compaction** | MicroCompact（清旧 tool 大输出）→ Session Memory 摘要 → Full LLM compact；规则放 `CLAUDE.md` 压缩后仍重注 | [Context window](https://code.claude.com/docs/en/context-window) / [Inside Compaction](https://y-agent.github.io/inside-claude-code/04-context-compaction.html) | **多级压缩 + 压缩后重注根状态**；稳定前缀利 Prompt Cache |
| **Raschka / Coding Agent 组件** | Full transcript（永不丢）+ Working memory（小而精喂模型）双轨 | [Components](https://magazine.sebastianraschka.com/p/components-of-a-coding-agent) | 我们 transcript 已有；缺独立 working memory 文件 |
| **OpenAI Cookbook** | Compaction（长会话）与 Memory（跨 run 可复用教训）分开 | [Memory & Compaction](https://developers.openai.com/cookbook/examples/agents_sdk/building_reliable_agents_memory_compaction) | 会话内压缩 ≠ 跨任务长期记忆 |
| **ACON** | 观测压缩 + 历史压缩；失败对优化压缩 guideline（完整版） | [论文](https://arxiv.org/abs/2510.00615) / [代码](https://github.com/microsoft/acon) | **已简化落地** `compress.py` + `context_manager.py`；不做离线优化循环 |
| **HiAgent / HyMem / HMAT** | 按子目标切块；计划与执行轨迹隔离；Working/Episodic/Semantic | HiAgent / HyMem 等论文 | Todo 完成一项时可触发「阶段压缩」 |
| **BYOHarness** | Compaction / Memory / Prompt caching 分章练习 | [byoharness.dev](http://www.byoharness.dev/) | 改造时可对照章节改 harness |

### 11.3 「TUI / 缓存优化」实际指什么

多数不是画终端的技巧，而是 **API Prompt Caching / 前缀稳定**：

1. **前缀稳定**：system、tools schema、项目规则放最前且少改字节  
2. **后缀可变**：Current State、最近 tool、用户新话放最后  
3. **先砍观测再 LLM 总结**：先 stub 大 stdout，再 compact（省钱）  
4. **压缩后重注**：规则 / 焦点文件从磁盘再注入，不依赖摘要「记得住」

现状：system 在单次 run 内缓存（工作区快照冻结）；Current State 作为**可变后缀**挂在消息末尾，不再插在 task 后面把前缀冲掉（P1 已改）。

### 11.4 RAG 该用在哪

| 适合 | 不适合硬上向量 RAG |
|------|-------------------|
| 大仓库语义搜、旧 session 语义召回、项目文档 | 当前报错、刚改文件、todo（应在 Working） |
| | 每步必遵的规则（应进 system / MEMORY.md） |

优先顺序：**文件工具检索 → 关键词搜 transcript → 再考虑 embedding**。

### 11.5 现状操作速查（改代码前先读）

```text
短期（单次 run）
  工具结果 → compress_tool_result → messages
  每步 → ContextManager.prepare_messages
    （稳定 system 前缀 + 末尾可变 working-memory 后缀；超预算折叠）
  todo → completed → 阶段压缩进 history_summary
  折叠后 → 重注 MEMORY + focus_files + 未完成 todo
  结束 → working_memory.json + transcript.memory；规则模板追加 MEMORY.md

会话（同 Web 聊天多轮）
  前端 session_id → load_session → build_continue_context
    （memory 快照 + 最近 K + 原任务）→ 再跑 loop；磁盘 messages 仍追加全量
  New task / Explore → 新 session；可读 MEMORY.md / working_memory.json

长期（跨会话）
  MEMORY.md 开局注入；memory_search 关键词搜 MEMORY + transcripts
  向量语义检索：本地 TF–IDF `rag_search`（P2）；云端 embedding 仍可选
```

关键文件：

- `src/agent/compress.py` — 观测压缩 + MicroCompact stub/summary/full  
- `src/agent/acon_guideline.py` — 失败对 → compress guideline  
- `src/agent/context_manager.py` — 分层 + microcompact + 折叠 + 重注  
- `src/agent/memory.py` — MEMORY.md + working_memory.json + keyword search  
- `src/agent/rag.py` — 本地 TF–IDF 索引 / rag_search  
- `src/agent/cache_policy.py` — prompt-cache 钩子 + 本地 compaction fallback  
- `src/tools/memory_search.py` / `src/tools/rag_search.py`  
- `src/agent/loop.py` / `src/web/runner.py` / `src/llm/client.py`  
- 配置：`CONTEXT_TOKEN_BUDGET` / `--context-budget`
- 模板：`MEMORY.md.example`（运行时 `.agent/*` 记忆文件已 gitignore）

### 11.6 后续改造清单（下次按这个做）

#### P0 — 性价比最高（建议下次优先）

- [x] **项目级 `MEMORY.md`（或 `.agent/MEMORY.md`）**  
  - 任务结束：把「改了哪些文件、结论、坑」追加写入（可用规则模板，不必每次调模型）  
  - 新任务开始：读前 N 行 / 最近一段，注入 System 或 Current State  
  - 落点建议：`context_manager.py` + `runner`/`loop` 收尾钩子；gitignore 可选保留模板  
- [x] **阶段压缩（对齐 Todo）**  
  - 某 todo → `completed` 时，将该项相关旧 tool 轨迹收成 3～5 条 bullet 进 `history_summary`  
- [x] **续写瘦身**  
  - Web 下一轮不要默认整包 `prior_messages`；改为：`memory` 快照 + 最近 K 条消息 + 原任务  

#### P1 — 体验与成本

- [x] **Prompt 布局：稳定前缀 + 可变后缀**（利 DeepSeek/兼容网关的 cache，若有）  
- [x] **独立 `working_memory.json`**：与 full transcript 双轨（Raschka 模式）  
- [x] **`memory_search` 工具**：先关键词/grep 搜 `transcripts/` 与 `MEMORY.md`，再考虑向量  
- [x] **压缩后重注根状态**：折叠后强制再注入 MEMORY + focus_files + 未完成 todo  

#### P2 — 重，可答辩提「未做 / 下一步」

- [x] **向量 RAG（代码库或历史 session）** — 本地 TF–IDF `rag_search`（无云端 embedding / 无 LangChain）  
- [x] **完整 ACON：失败对 → 更新压缩 guideline** — `.agent/compress_guideline.json`（不做离线蒸馏小模型）  
- [x] **MicroCompact 分级** — stub → summary → full；超 75% budget 先 stub 旧 tool 再 fold  
- [x] **KV-cache / 服务端 compaction** — `cache_policy` 挂 prompt_cache_key；无厂商端点时本地 fold fallback
### 11.7 改造时注意（防踩坑）

1. **交付期**：第 6 节 Day4「当天不再加新大功能」——记忆增强默认放交付后。  
2. **勿用框架**：可用自研小工具 + 文件；不要引入 LangChain Memory。  
3. **答辩一句话**：短记忆靠压缩与分层；长记忆靠可编辑外存 + 检索注入；RAG 是手段不是全部。  
4. **改完后**：更新本节勾选、`README` 一行、冒烟加 MEMORY 读写用例、进度日志追加一条。

---

## 12. 进度日志

> 每次修改代码后追加一条。格式：日期 — 做了什么 — 勾选了哪些任务 — 下一步。

### 2026-08-29（十六）长短期记忆 P2

- 本地 TF–IDF `rag_search`（代码 / MEMORY / transcripts → `.agent/rag_index.json`）。  
- ACON-lite：失败对写入并收紧 `.agent/compress_guideline.json`（不蒸馏小模型）。  
- MicroCompact：stub / summary / full；超 75% budget 先 stub 旧 tool 再 fold。  
- `cache_policy`：prompt_cache_key 经 `extra_body` 下发；无厂商 compaction 时本地 fold fallback。  
- 冒烟 / README / 第 11.6 P2 勾选已更新。  
- **下一步：** 交付录视频 / README.txt / push。

### 2026-08-29（十五）长短期记忆 P1

- Prompt 布局：system 单次 run 内缓存（稳定前缀）；Current State 改为消息**末尾可变后缀**。  
- 双轨：`workdir/.agent/working_memory.json`（+ transcripts 镜像）与 full transcript 分离；新 run 可 hydrate。  
- 新工具 `memory_search`：关键词搜 MEMORY.md + transcripts。  
- 折叠后重注 Root State（MEMORY + focus_files + 未完成 todo）。  
- 冒烟 / README / 第 11.6 P1 勾选已更新。  
- **下一步：** 交付录视频 / README.txt / push；P2（向量 RAG 等）可答辩提未做。

### 2026-08-29（十四）长短期记忆 P0

- 新增 `src/agent/memory.py`：`MEMORY.md` / `.agent/MEMORY.md` 开局注入、结束规则模板追加；模板 `MEMORY.md.example`，运行时文件 gitignore。  
- Context Manager：加载 Project Memory；todo → `completed` 时阶段压缩进 `history_summary`。  
- Web 续写：`build_continue_context`（memory 快照 + 最近 K + 原任务）；session 磁盘仍追加全量 messages。  
- 冒烟覆盖 MEMORY 读写 / 阶段压缩 / 续写瘦身；README + 第 11.6 P0 勾选。  
- **下一步：** 交付录视频 / README.txt / push；有余力再做 P1（前缀缓存友好 / `memory_search`）。

### 2026-08-29（十三）计划书写入「长短期记忆改造路线」

- 新增 **第 11 节**：业界分层、MemGPT/Claude Compact/ACON/RAG 结论、TUI/缓存含义、P0–P2 改造清单与落点文件。  
- 原「进度日志」改为第 12 节；面试预案补第 8 问。  
- **下一步：** 交付录视频 / README.txt / push；有余力再做第 11.6 节 P0（MEMORY.md）。

### 2026-08-29（十二）ACON 简化版 Context Manager

- 参考 [ACON](https://arxiv.org/abs/2510.00615) / [microsoft/acon](https://github.com/microsoft/acon)：不做失败对优化循环，落地工程版压缩器。  
- 新增 `src/agent/compress.py`（pytest/shell 观测压缩）+ `src/agent/context_manager.py`（五层：System / Task / Current State / Recent / Historical）。  
- Loop：工具结果先压缩再入 messages；超 `CONTEXT_TOKEN_BUDGET` 折叠中间轮为摘要；transcript 写入 `memory`。  
- 冒烟覆盖压缩与 prepare_messages；计划书 / README / 面试答「上下文爆了」已更新。  
- **下一步：** 录视频 → README.txt → push。

### 2026-08-29（十一）Web UI：侧栏固定 / Open folder / Files / Diff / Session 历史

- 左侧栏 `sticky` 不随主区滚动；**Recent chats** 独立滚动条。  
- **一大轮对话 = 一条 session 历史**（`session_*.json`）：同会话多轮续写会带上 prior messages，可供后续长短期记忆处理。  
- 顶部 **Open folder**：浏览本地目录并设为 workdir；右侧 **Files** 树可展开/折叠，点击文件新窗口预览。  
- `write_file` / `edit_file` 发出 `file_change` 事件；底部 **Changed files**，点击弹窗对比新旧内容。  
- 离线冒烟 `smoke_v1` 通过。  
- **下一步（交付）：** 录视频 → 写 README.txt → push → 打 zip。

### 2026-08-29（十）计划书「已完成总览」+ UI 富文本/折叠

- 计划书新增 **第 0 节「已完成总览」**：对照题目硬性要求、能力清单、未完成交付项、速查命令。  
- Web：Markdown 富文本；完成步骤自动折叠；进行中步骤滚到视口中央并高亮。  
- **下一步（交付）：** 录视频 → 写 README.txt → push 最新代码 → 打 zip 提交。

### 2026-08-29（九）结构化事件 + UI 步骤卡片

- `run_agent` 增加 `on_event`：`step_start` / `tool_call` / `tool_result` / `todo_update` / `final` 等。  
- Web：SSE 推送结构化事件；单任务锁；`GET /api/history/{id}` 回看；`POST /api/demos/reset` 恢复演示 bug。  
- 前端：步骤卡片、Todo 面板、Running 状态、历史回放；静态资源 `?v=` 防缓存。  
- **下一步：** 浏览器硬刷新 http://127.0.0.1:7860 试跑 greeter；录视频；Day4 提交物。

### 2026-08-29（八）Web UI

- 新增 `src/web/`：FastAPI + 静态前端（侧栏 / 问候 / 快捷卡片 / 底部输入，风格参考现代 AI 聊天 UI）。  
- `/api/run` 以 SSE 推送 tool 日志；底层复用 `run_coding_task`（同一套自研 harness，非套壳第三方 agent）。  
- 启动：`python -m src.web` → http://127.0.0.1:7860  
- **下一步：** 本地打开界面试跑演示任务；录视频；Day4 写 README.txt / 提交；可选 push GitHub。

### 2026-08-29（七）Day3 Todo 特色 + 演示定稿

- 实现 `todo_write`（`src/tools/todo.py`）+ system prompt「先计划后执行」约束。  
- 演示：`demos/greeter.py` + `greeter_test.py` + `DEMO.md`；README 补架构说明。  
- 端到端 5 步通过（todo → 读 → edit → 测试 ok → todo 完成）；源码已恢复为有意 bug。  
- **下一步（你来做）：** 按 `demos/DEMO.md` 录 ≤2 分钟视频；然后 Day4 写 README.txt、打 zip 提交。可选：把 Day2/Day3 代码 push 到 GitHub。

### 2026-08-29（六）Day2 Harness 合格线

- 新增：`edit_file`、`glob`；`PermissionGate.authorize`（hard-deny + auto/ask/never）；`trim_messages`；`save_transcript`。  
- CLI：`--approval` / `--max-messages` / `--transcript-dir`。  
- 离线冒烟通过；端到端修复 `buggy_calc.py` 通过（演示文件已恢复为有意 bug，便于复测）。  
- **下一步：** Day3 — 选定并实现一个特色（建议 Todo 或测试驱动修复）+ 定稿演示任务。

### 2026-08-29（五）推送到 GitHub

- 本地 `git init`，首次提交 `5faabcb`（不含 `.env`、不含考核 PDF）。  
- 已推送到公开仓库：https://github.com/hyj321/coding-agent （`main`）。  
- **下一步：** Day2 — `edit_file`、上下文裁剪、approval、transcript。

### 2026-08-29（四）V1 端到端验收通过

- Key 写在了 `.env.example`，已复制到 `.env`，并清空 example 中的密钥，避免误提交。  
- 真实任务：在 `demos/` 创建 `hello.py` 并 `python` 运行 → **3 步完成**（write_file → run_shell → 最终回复）。  
- **下一步：** Day2 — `edit_file`、上下文裁剪、approval、transcript。

### 2026-08-29（三）V1 代码落地

- 初始化项目骨架：`src/`（config / agent / llm / tools）、`requirements.txt`、`.env.example`、`.gitignore`、`README.md`。  
- 实现：`ToolRegistry`、`read_file` / `write_file` / `list_dir` / `run_shell`、路径沙箱、`LLMClient`、`run_agent` 主循环、CLI。  
- 扩展点：新工具只需注册；Permissions / Context 独立模块，便于 Day2 加 approval、裁剪、transcript。  
- 离线冒烟 `python -m scripts.smoke_v1`：**通过**。  
- **下一步：** 你配置 `DEEPSEEK_API_KEY` 后跑真实任务验收 → 通过后进入 Day2（`edit_file`、上下文裁剪、approval、transcript）。

### 2026-08-29（二）调研更新

- 调研并对比：ai-coding-agent、CoreCoder、mini-coding-agent、learn-claude-code、FutureAGI harness 文、Edd Mann 实践文等。  
- 结论写入本计划：以 **六层 harness** 叙事；开发顺序对齐 **v1 → 稳定 → 单特色**；明确「学架构不抄仓」。  
- 特色候选调整为更贴开源共识的 **Todo 规划 / 测试驱动修复 / 循环检测**。  
- 技术倾向：Python + DeepSeek tool calling。  

### 2026-08-29（一）初稿

- 完成计划书初稿，明确组件、必备/扩展功能与四天节奏。
